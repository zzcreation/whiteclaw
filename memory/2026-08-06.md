# 2026-08-06

- `zzc-cloud-server` 对照飞书《ZZC 双流体验系统-供应商需求》revision 90，将最终供应商接口契约 V2 写回 `docs/PRD.md` 与 `docs/current-task-checklist.md`。
- 产品口径确定为独立的 `gameSessionId` + `interactionPointCode`、供应商 `ACCEPTED` 后计次、成绩回传不计次、完全相同重试返回 200 `ALREADY_PROCESSED`、不同 payload 返回 409、会话过期返回 410。
- 运营模式改为三方主机粒度；全园切换是逐台批量操作，手动模式不影响入口激活、选择展项和既有出场链路。
- 文档提交 `8c0a44d`（`docs: align vendor contract v2`）已推送到远端 `dev`。
- 回顾群聊后确认仍有已拍板但尚未写回 PRD/Checklist 的口径：未游玩不评级、总评级只看已玩游戏展项、覆盖率含启用的游戏与选择展项并排除人工/不可用且未玩点位、GET achievements 只读 + POST checkout、稳定任务 URL、一次性手环 CLOSED 终态、选择项按 selectedAt、主题分区本期仅作档案层级。
- 文档决策补齐提交 `f04b3dc`，P0 正确性修复提交 `c9cde01`；评分 0/未评级、成绩不计次、影片顺序/缺素材、selectedAt、RFID 响应时序均已落地。
- 三方主链路连续提交并推送：`012d4bf`（vendor_host、点位 code、game_sessions 底座）、`1bdd598`（供应商 ACCEPTED 后激活计次）、`d772ad1`（V2 回传幂等/409/410）、`821f6a2`（主机模式）、`6f64b6e`（健康探测、MANUAL 拦截与受影响 code）。
- 最新 Pipeline #678 全绿，verify/build-image/deploy-sandbox 均成功；本地全量测试增至 82+，工作区干净。
- 仍未完成且不可混淆：玩家成绩迁 MySQL 后的会话+成绩真原子事务、PROCESSING 崩溃恢复与 ≥7 天清理、RFID 失败码细分、签名/完整 Golden Fixture。
- 2026-08-06 复核前后端闭环能力：当前只支持核心子链路测试，尚不能宣称“前后端完整闭环验收”。后端 `6f64b6e` / Pipeline #678 全绿，已覆盖会话创建、供应商 ACCEPTED 后计次、V2 成绩幂等及主机模式/健康的服务级测试。
- 前端当前 `0565352`，本地 typecheck、10 条集成测试、构建、2 条 Playwright E2E 均通过，最新 GitHub CI（含部署与 Sandbox smoke）成功；但浏览器 E2E 使用 mock API，真实 Sandbox smoke 只覆盖登录、游戏组 CRUD、空影片配置完整性和 active 切换，未覆盖游客、RFID、供应商启动、成绩回传或成片。
- 前端仍使用点位级 `vendorBaseUrl/healthUrl/timeout/sharedSecret` 配置，尚未适配后端 `vendor_host`、`interactionPointCode + vendorHostId` 新契约，因此当前 UI 的游戏设备注册不能作为可靠的新后端验收路径。
- 出场闭环仍缺：只读 achievements + 幂等 checkout、CLOSED 终态、覆盖率快照、唯一影片任务、稳定 `jobId/resultUrl` 与二维码；成绩仍在 Redis、会话在 MySQL，尚无跨存储真原子性。
- 建议下一步先做“前后端契约重新对齐 + Sandbox Golden 主链路”：前端补 vendor host、点位 code/host 关联、模式/健康；部署可控供应商桩，将激活→RFID→ACCEPTED→成绩→玩家统计跑成真实自动回归。之后实现 checkout/稳定结果地址，再补最后一段完整闭环验收。
- 后端随后完成出场闭环与 Sandbox Golden，并将拆分后的任务状态回写 `docs/current-task-checklist.md`；`dev` 最新提交 `6ac559d`，GitLab Pipeline #694 的 verify、build-image、deploy-sandbox、sandbox-golden 全部成功。
- 已可自动验证真实核心闭环：激活手环 → TCP RFID → 供应商 `ACCEPTED` → 计次 → V2 成绩回传/幂等 → achievements 只读 → 并发 checkout 收敛为同一任务 → 玩家 `CLOSED` → 覆盖率快照 → FFmpeg 生成 MP4 → 稳定结果地址 303 跳转。
- 本批关键提交：`a768fc7`（幂等 checkout、数据库唯一任务、稳定结果地址）、`938ee95` / `8fed824`（出场 Golden 与真实并发 checkout）、`147a496`（任务状态/快照持久化、失败重试、定时崩溃恢复、2 路生成并发）、`7c1a99e` / `572dfff`（真实 MP4 出片与稳定 303）、`6ac559d`（Checklist 状态）。
- Golden 还覆盖同请求重试、不同 payload 冲突、过期会话、MANUAL 模式、多点位路由、供应商失败提示、Redis 任务副本丢失、生成失败重试和最终成功出片。因此当前可表述为“核心功能支持完整闭环测试”，但不能表述为生产就绪。
- 仍需保留的生产化工作：真实七牛凭证环境上传 Smoke、3 分钟生成链路 P95 压测、玩家/游玩数据全量迁 MySQL 后的会话+成绩真原子事务、会话清理、签名校验与 RFID 失败码细分；下一批优先做数据迁移与原子性闭环。
- `zzc-cloud-server` Sandbox Docker 镜像堆积已治理：提交 `7bce1a4` 为 `dev` CI 增加部署/Golden 后的镜像清理阶段，提交 `1396298` 修正同一 image ID 挂多个 SHA tag 时的重复标签清理；两笔均已推送。
- GitLab Pipeline #696 全绿，verify、build、deploy、Sandbox Golden、cleanup 全部通过。首次清理移除后端历史 tag 48 个，镜像占用由 18.18GB 降至 16.12GB（约释放 2.06GB）；修正版再移除后端重复 tag 4 个和 vendor-stub 历史 tag 14 个。
- 当前本项目在 Sandbox 仅保留后端 2 个 tag、vendor-stub 1 个 tag。以后每次 `dev` CI 都与 Sandbox 部署串行清理：保护运行中容器的精确 tag，另保留最近两版唯一镜像；不操作其他项目镜像、容器、volume 或全局 build cache。
- Sandbox Docker daemon 仍有约 10.97GB 共享 build cache；因可能影响其他项目并行构建，本次未清。若后续治理，应单独制定按年龄过滤的共享缓存清理策略。
- 新增 `docs/reference/DI20-WY2-tech-notes.md`：从 DI20-WY2 使用说明 PDF 提炼刷卡器 TCP 契约（JSON sn/uid、ACK `DI20_Y_V21_SUC`、2s 超时、30s `0x00` 心跳、蜂鸣/LED），并对照现有 `UserRfidServerHandler` 标出心跳过滤与 idle 竞态等实现缺口。
