# 2026-08-05

- `zzc-sl-admin` 完成影片配置与视频素材库前端：VideoSequenceConfig 创建/复制/编辑/排序/删除、素材完整性检查与 active 切换；素材列表、鉴权预览、上传进度、同名覆盖确认、删除及缺失矩阵。
- 新增影片管理集成测试与 Playwright E2E；本地 8 条集成测试、2 条 E2E、类型检查和生产构建全部通过。
- 前端提交 `38936e2` 已推送 GitHub；Actions `30962730842` 的 verify 与 Playwright E2E 全绿。
- `zzc-cloud-server` 完成 SEC-01～04 安全收口：游客自动建档改 BCrypt、增加可开关的存量明文迁移器；敏感配置取消可用默认值并由 GitLab 变量注入；RFID 解析从 fastjson 1.2.53 替换为 Jackson 严格校验；确认素材路径白名单与回归测试已存在。
- 后端提交 `e8b2c3d` 已推送 `dev`；GitLab 流水线 #655 的 41 条测试、镜像构建与 Sandbox 部署均成功。部署后健康检查 200、未授权 GM 接口 401。
- `39.106.154.140` 的 80/443 由宿主机独立安装的 Nginx 管理（不是 GitLab Omnibus 内置 Nginx）；后续可直接新增 `api-sandbox.zzcreation.com` 虚拟主机，反代到 `127.0.0.1:3202`。
- 原计划的两级子域名 `sandbox.sl-api.zzcreation.com` 不在 `*.zzcreation.com` 证书覆盖范围内，已改为 `sl-api-sandbox.zzcreation.com`。新域名解析到 `39.106.154.140`，HTTP 301、TLS、真实健康接口（UP）和未授权 GM 401 均正常；CORS 预检当前返回 401，待后端配置。
- Sandbox 前端最终域名确定为 `https://sl-admin-sandbox.zzcreation.com`；Cloudflare Pages 项目为 `zzc-sl-admin-sandbox`，默认域名 `https://zzc-sl-admin-sandbox.pages.dev`。
- 已创建 Sandbox 测试管理员 `sandbox-test-admin` 并验证真实登录 200；密码仅通过飞书私聊发送，未写入仓库或记忆文件。
- GitLab Sandbox CORS 来源已更新为自定义前端域名、Pages 默认域名和本地开发地址，并触发流水线 #657 重新部署。
- 完成管理员安全闭环：服务端登出与 tokenVersion 全量吊销、禁用账户、改密、管理员鉴权创建、密码哈希脱敏、公开路径统一配置，并将 Actuator 健康输出收敛为仅 `{"status":"UP"}`；后端主要提交 `68c7389`，配置修复 `74e97c8` / `6e71b44`。
- 真实 Sandbox smoke 暴露原有 R2DBC 自动 UUID 返回不稳定问题，导致创建后更新 404；后端改为服务端显式 UUID + insert 语义，覆盖游戏组、选择组和设备，提交 `0806093`，GitLab 流水线 #662 全绿并部署。
- 前端新增管理员账户页、服务端登出、专用 CI 管理员 Secret 和发布后真实 Sandbox smoke；提交 `bbccfe5`、`91e55ec`、`0565352`。smoke 覆盖真实登录、管理员页、游戏组 CRUD、影片配置、素材完整性、active 切换/恢复，并强制清理 `e2e-` 数据。
- GitHub Actions `30993425681` 全绿：类型检查/10 条集成测试/生产构建、2 条模拟 E2E、Cloudflare Pages 部署、真实 Sandbox smoke 均成功。线上复测前端 200、健康 200、未授权管理员接口 401、可信来源 CORS 预检 200。
- 后端新增 `docs/current-task-checklist.md`，提交 `37f7334`，随后按代码真实状态校正并持续更新；计分/评级、展项聚合和 `_Z` 选片确认已在 8 月 4 日基线完成。
- 完成手环生命周期第一批：`UNACTIVATED → ACTIVATED → CLOSED`、公开状态查询和激活接口、重复激活幂等、关闭后禁止重开、RFID/成绩/选择交互统一要求 `ACTIVATED`，状态写入失败进入响应链路；提交 `c804d1f`、`dbf8888`、`1676ed6`。
- GitLab #667 测试、镜像和 Sandbox 部署全绿；线上验证未知手环状态返回 `UNACTIVATED`，未激活成绩上报返回 409，健康接口 200。Checklist 更新提交 `b91174f`。
- 对照新提交的 `docs/architecture-review-2026-08-05.md` 复核当前前后端进度，将仍未解决和新发现的问题同步到 `docs/current-task-checklist.md`，提交 `9c4c33f`。最高优先级调整为 level-0 影片回归、刷卡/成绩双计次、orderIndex、缺素材静默截片、选择时间、RFID 提前回成功与 TCP 响应关联；另补出场 checkout 设计、稳定二维码 URL、吞吐指标、现场协议决策、Flyway/MySQL、文档治理和 Golden Fixture。
- Review 中已过时的两项未作为未完成项照抄：`security.public-paths` 已被 SecurityConfig/JWT Filter 共用；当前 RFID 活跃路径已不再执行旧的同步票务校验，但废弃方法与 dev URL 仍列为清理项。
