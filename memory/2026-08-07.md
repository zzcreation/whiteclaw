# 2026-08-07

- `zzc-cloud-server` 增加 DI20 单字节 ASCII `0`（`0x30`）心跳兼容，同时保留 `0x00`；单测、Sandbox Golden、PRD/Backlog/Checklist 已同步。提交 `b42c5e0` 推送到 `dev`，GitLab Pipeline #700 最终全绿。
- Sandbox Golden 首次在既有视频重试等待窗口停留于 `PROCESSING`，重跑成功；本批新增的 `0x00` / `0x30` 真实 TCP 无 ACK 验证两次均通过。
- `zzc-sl-admin` 手测 README 将公网 DI20 端口从 `3105` 修正为实际 `3205`，并补两种心跳说明。提交 `e73a155` 推送到 `main`，GitHub Actions run `31140420907` 重跑后全绿，Cloudflare 部署与认证 Sandbox smoke 成功。
- 从公网实测 `39.106.154.140:3205`：`0x00` 与 `0x30` 均在 1 秒内无 ACK、连接未关闭；Sandbox API health 与管理后台均 HTTP 200。
- 七牛手测前置核对：项目 CI 已配置并掩码保存 `SANDBOX_QINIU_ACCESS_KEY` / `SANDBOX_QINIU_SECRET_KEY`，另有 Bucket/上传前缀变量；但 `deploy/compose.sandbox.yml` 仍固定关闭云上传并使用 `sandbox.invalid`，真实上传 Smoke 需要一次临时受控启用和测试对象清理。
- 为多 Sandbox 共用的 Docker 主机建立 `zzc-cloud-sandbox` 强制命令 SSH 网关：专用用户 `openclaw-zzc` 不在 docker 组；只读密钥仅可查看 4 个项目容器状态与 bounded logs；运维密钥额外只可重启 backend/vendor-stub。实测任意命令和只读密钥 restart 均被拒绝。
- 通过新日志网关复核现场刷卡：11:10～11:17 两个真实 SN `DI20240731000002` / `DI20260803000002` 均上报原始 `20B30E14`，该值只出现在 Netty LoggingHandler 入站日志；业务随后返回 `Bracelet is not activated` 和 `-1`，与十进制 UID 转换逻辑未变的判断一致。
