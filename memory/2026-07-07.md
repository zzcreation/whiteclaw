- OpenClaw sandbox browser 修复：按用户要求采用“官方基础镜像、单测试镜像、Docker 单测先行、可回滚”方式，新建 `tmp/sandbox-browser-playwright-test/`，构建 `openclaw-sandbox-browser-playwright:test-20260707`，基础镜像为 `mcr.microsoft.com/playwright:v1.56.1-noble`。Docker 单容器测试通过：`/json/version` 返回 `Chrome/141.0.7390.37`，没有 Chromium core dump。随后备份 `/home/zzc/.openclaw/openclaw.json` 为 `openclaw.json.bak-20260707-browser-playwright-test`，将 `browser-sandbox` 的 `sandbox.docker.image` 和 `sandbox.browser.image` 都指向该测试镜像；`openclaw sandbox recreate --agent browser-sandbox --force` 后，OpenClaw 子任务验证通过：能打开 `https://example.com`，snapshot 和 screenshot 均成功。注意：OpenClaw 架构仍会创建普通 sandbox + browser sandbox 两个容器，但使用同一个镜像；回滚方式是恢复备份或移除这两个 image 字段。
- `gem_merchant_web` 测试策略决策：OpenClaw sandbox browser 不作为日常快速开发主循环，而作为关键节点隔离验收工具。日常开发优先用 `npm test`、host browser、本地 `wrangler dev`；联机功能验收使用 host browser + sandbox browser 模拟两个独立浏览器环境。控制浏览器验证类子 agent 并发，避免 Docker Desktop/WSL、容器冷启动、CDP readiness、多容器生命周期和截图资源开销导致超时。已把该策略写入 `projects/zzc/gem_merchant_web/docs/CHECKLIST.md` 和 `docs/PRODUCT.md`。
- `gem_merchant_web` 里程碑 3 第一版后端骨架：新增 `worker/room.ts` 房间控制器，接入 `room.join`、`room.ready`、`room.start`、`game.action`，服务端使用现有 `createInitialGameState`、`applyGameAction`、`createClientGameView`；更新 `worker/index.ts` 提供 `/api/rooms/:roomCode/websocket` Durable Object WebSocket 路由，`wrangler.toml` 增加 `ROOMS` Durable Object binding 和 `GameRoom` migration；扩展 server protocol 增加 `room.joined`、`room.lobby`；新增 `tests/worker/room.test.ts` 覆盖加入、准备、开始、行动广播、版本过期错误和断开连接；README/CHECKLIST 同步当前阶段。`npm run test` 6 个文件 18 条通过，`npm run build` 与 `npm run lint` 通过；`wrangler deploy --dry-run --outdir /tmp/gem-merchant-worker-dry-run` 通过并识别 `env.ROOMS (GameRoom)` Durable Object binding，未实际部署。下一步：前端接 WebSocket 房间状态，并补 wrangler dev / 双浏览器联机验收。
- `gem_merchant_web` 里程碑 3 继续：前端 `src/App.tsx` 增加房间码/昵称连接栏、加入/本地切换、ready/start 控制；在线模式下 `game.action` 通过 WebSocket 发送到 Worker，UI 由服务端 `state.snapshot` / `state.patch` 驱动，本地 mock 模式保留。修正 `GameRoom` 不能用 Durable Object id 当房间码的问题，改为从 `/api/rooms/:roomCode/websocket` URL 解析 roomCode。`wrangler dev --ip 127.0.0.1 --port 8787` smoke test 通过：两个 Node WebSocket 客户端加入 `SMOKE1`、ready、start 后收到 `phase=playing`、players=2、version=2；临时 dev 进程已清理。`npm run test` 6 文件 18 条通过，`npm run build` 通过，`npm run lint` 通过；`wrangler deploy --dry-run` 通过。下一步：断线重连持久化、Hibernation、host+sandbox 双浏览器验收。
