# 2026-09-03

## Zzc Comfy Cloud 项目
- 项目仓库：`/home/zzc/.openclaw/workspace/main/projects/zzc/zzc-comfy-cloud`，当前开发分支为 `draft/phase-0`。
- 产品目标：Cloudflare 上的 Web/API 调度本地 ComfyUI；正式连接器应为 Windows 原生 Go 单文件 `connector.exe`，默认安装在 ComfyUI 所在的 Windows 主机并作为 Windows Service 常驻。连接器与 ComfyUI 分居同一 LAN 技术上可行，但不属于 Phase 0 默认交付范围。
- 主 OpenClaw/当前 WSL 所属 Windows 主机是 `192.168.130.29`；ComfyUI/RTX 3090 Windows 主机是 `192.168.130.229`，ComfyUI 监听 `192.168.130.229:8188`。两台机器不要混淆。
- 云端已部署至 `https://ai-tool.hezzze.com`，原 workers.dev 地址为 `https://zzc-comfy-cloud-api.zzcreation2022.workers.dev`。D1 数据库 `zzc-comfy-cloud` 已迁移，R2 bucket 已创建。
- 已上线并验证 Web/API、管理员登录、调度、LAN 连接器、ComfyUI 执行、产物回传闭环；已有 5 个 Workflow，随后集成了 `workflow_samples/minimax-h3.json`，成为第 6 个预设。
- 已修复的生产问题包括：Workers PBKDF2 上限、空闲心跳字段遗漏、ComfyUI 失败 history 被误判成功、图片解析文本结果未上传、生成图片无法预览相关的前端展示/资源访问链路。
- 前端核心能力正在优先完善：输入图预览、任务缩略图、图片/视频详情预览和原文件入口已实现；MiniMax H3 的 URL 图片输入、提示词、时长、比例映射已接入。
- 资源存储方向：默认改为七牛云，配置需包含 bucket、CDN URL、上传域名和路径前缀；参考仓库 `ref/` 中的配置方式。云端签发短时直传凭证，长期 AK/SK 不下发到连接器。R2 暂保留为兼容/回退；若仅用于生成产物且七牛完成替换，可暂时不用。用户会在实现完成后通过安全方式填写七牛 credential，聊天中不得索取或回显凭据。
- 当前为了维持在线任务，`.29` 的 WSL 内有正式持久化 systemd 用户服务 `zzc-comfy-cloud-connector.service`，已 enabled/active，带代理和自动重启。这只是联调桥接，不算 Windows 连接器正式交付；Windows 服务在 `.229` 真实验收通过后才能退出 WSL 桥接。
- Windows 原生连接器第一轮骨架已完成：基于 Go `x/sys/windows/svc` 接入 Service 生命周期，可交叉编译 64 位 `connector.exe`；规划/实现命令包括 `run / doctor / status / version / install-service / uninstall-service`，并支持配置文件、显式代理、ACL/日志/恢复策略。最终安装 Windows Service 需要在 `.229` 上执行一次管理员操作/UAC，不需要开放长期 RDP/SSH/WinRM。
- 项目根目录已有 `backlog.md`，记录已完成项、偏差、P0 Windows 连接器纠偏计划、可靠性/调度缺口、测试现状和下一步；需要持续更新。
- 上线验收仍需完成：登录后的真实 UI 检查（历史图片缩略图、任务详情大图/视频、原文件入口、MiniMax 表单、移动端布局）、清理临时测试会话、更新 backlog，并提交当前前端/七牛/MiniMax 这轮代码。
- 已知测试状态：TypeScript 类型检查和 Go 测试通过；API 尚缺 Vitest 测试，仓库根目录 `npm test` 曾仍失败，属于 backlog 中的待办。

## OpenClaw / Cloudflare 环境经验
- OpenClaw 2026.8.2 在当前 explicit-ownership 多 Agent 配置下将 `main` 的工作区解析为 `~/.openclaw/workspace/main`。已将原 `~/.openclaw/workspace/` 中除目标 `main/` 外的全部内容（包含 `projects/` 与历史 `.git`）迁入 `workspace/main/`；旧根现在只剩 `main/`。`main` 和 `browser-sandbox` 的 workspace 均已显式固定到该路径，迁移前由 8.2 新建的空仓库/默认模板保存在 `~/.openclaw/backups/workspace-main-migration-20260903-1308/preexisting-main/`。
- `~/.openclaw/.env` 不会覆盖 Gateway 进程中已存在的同名环境变量。Linux systemd unit 会先读取 `~/.openclaw/gateway.systemd.env`，因此仅修改 `.env` 后执行 `gateway restart` 可能继续沿用旧快照。
- 本次通过 `env -u CLOUDFLARE_API_TOKEN -u CLOUDFLARE_ACCOUNT_ID openclaw gateway install --force` 重新生成服务环境，再重启 Gateway，已确认进程 Token 指纹与 `.env` 一致并经 Cloudflare verify 为 Active。
- Cloudflare D1、R2 已启用并可用；不要在日志、命令参数、聊天或记忆中记录任何 Token、AK/SK、管理员密码等秘密。

## 13:55 增量记录
- OpenClaw 8.2 工作区迁移已完成并复验：主工作区为 `/home/zzc/.openclaw/workspace/main`，项目仓库完整位于 `/home/zzc/.openclaw/workspace/main/projects/zzc/zzc-comfy-cloud`；分支仍为 `draft/phase-0`，历史 Git 仓库、远端、`backlog.md` 和未提交改动均保留。迁移后记忆索引已全量重建，423 个文件，`dirty=false`，memory search 已恢复。
- 群聊上下文并非完整群史自动注入：单轮通常只带截断的近期消息；当前 session 历史可分页读取，但需要主动调用。此前“项目目录不见”的判断是没有检查新 cwd 的 sibling/绝对路径所致，不是文件丢失。
- MiniMax H3 最新故障的已确认边界：用户点击提交后，云端没有产生新的 task，也没有产生新的上传 asset；连接器心跳正常，ComfyUI 健康且队列为空，MiniMax 预设和节点能力均存在。因此故障发生在浏览器端提交前或图片上传前，不是 ComfyUI/模型执行失败。
- MiniMax H3 下一步诊断：让用户再次点击“开始生成”并提供页面红色错误原文/截图，同时查看只筛 POST 的实时日志。代码中首要怀疑点是浏览器选择的图片 MIME 为空时被改成 `application/octet-stream`，随后遭上传接口拒绝；另一可能是必填图片/提示词前端校验未通过，导致根本未发请求。尚未得到复现错误前不要把任一猜测写成已定根因。

## 15:20 七牛区域修复
- 七牛上传签名 padding 热修复后，Kodo 已能识别凭证与 bucket，并明确返回 `incorrect region, please use up-z1.qiniup.com`。生产 bucket `zzc-mini` 位于华南，上传端点已从通用域名改为 `https://up-z1.qiniup.com`。
- 修改已部署到 `https://ai-tool.hezzze.com`，Worker 版本 `ba7abd0f-7f17-4846-b8dd-1e4bbb1f9495`，Health 正常；TypeScript、Vitest 4/4、Go 测试均通过。项目提交为 `a30edb1`，已推送 `draft/phase-0`。
- 仍需用户重新上传小图完成七牛端到端验收；旧错误请求不会自动重试。

## 15:50 七牛产物回传连接器修复
- 最新 MiniMax H3 任务 `627e44b4-e497-495d-ae72-cbced09b41b1` 已成功使用七牛输入图并在 ComfyUI 生成视频，但产物回传时报 `Put \"\": unsupported protocol scheme \"\"`。根因不是七牛：`.29` WSL 常驻服务仍运行 11:01 构建的旧连接器，只识别顶层 `uploadUrl`，无法解析七牛响应中的嵌套 `upload.url`。
- 已从当前源码重新构建 Linux 连接器并替换 `/home/zzc/.local/lib/zzc-comfy-cloud/connector`，原二进制备份为同目录 `connector.previous`；服务重启后 `enabled + active`，云端心跳正常。
- Go 回归测试新增完整链路：解析嵌套七牛上传槽位、multipart 上传并调用 complete；缺失 URL 改为明确错误而非执行空地址请求。Go 测试及 Windows amd64 交叉编译通过，提交 `1b73fe1` 已推送 `draft/phase-0`。
- 原失败任务 attempt 已终态，不能复用旧租约；需要重新提交一次 MiniMax H3 才能完成产物上传与私有 CDN 预览的真实验收。

## 16:10 七牛与 MiniMax H3 验收完成
- 最新 MiniMax H3 任务 `9b23f2a8-ebcc-441e-a577-62f883bd0684` 成功完成。输入 PNG 与输出 MP4 均为七牛 `ready`；输出对象位于 `zzc-comfy-cloud/prod/tasks/...`，大小 1,799,772 字节。连接器在线且空闲。
- 七牛链路已完成真实闭环：浏览器输入直传、私有 URL 供 ComfyUI 读取、连接器 multipart 产物回传、云端对象校验和页面预览。`backlog.md` 已标记完成并调整下一执行顺序，提交 `60b57b3` 已推送。
- 下一优先级遵循用户此前“前端核心优先”：完成其余 5 个 Workflow 实跑，并补失败一键重试、取消与可读错误详情；随后在 `.229` 安装 Windows 原生连接器并替换 WSL 桥接。

## 16:21 普通账户管理上线
- 用户要求优先支持管理员创建普通账户供他人使用。现有骨架已有 `user` 角色和管理员中间件，本轮补齐正式管理体验：管理后台用户页提供中文创建表单、初始密码确认、重复邮箱提示和现有账户列表。
- 创建接口现在固定写入 `role=user`，使用严格 schema 拒绝请求体注入 `role=admin`；普通用户前端不显示管理后台且渲染层有角色兜底，后端 `/api/admin/*` 继续强制管理员权限。任务、资产查询仍按当前 user_id 隔离。
- TypeScript 检查、Web 生产构建通过；API Vitest 扩展为 3 个文件、9 个用例，覆盖普通账户输入与管理员权限边界。已部署 Worker `cf2d30ca-0c5d-4358-9f19-acb0bec7c904`，线上 Health/首页 200，未登录访问管理员接口为 401，生产 bundle 已包含新界面。
- `backlog.md` 已更新，提交 `fa92258` 已推送 `draft/phase-0`。未代用户创建实际账户，因为账号凭据不应通过群聊或工具日志传递；由管理员在页面输入并安全交付给使用者。

## 19:25 任务取消、重试与错误详情上线
- 用户确认继续下一步开发后，按既定顺序完成任务操作闭环：排队/运行任务可取消；失败、已取消、取消未确认、无法调度任务可按原 Workflow 版本和参数创建新任务；前端显示中文错误摘要并保留可展开技术详情。
- 取消协议已贯通 API → ConnectorSession DO → Go 连接器 → ComfyUI。待执行 prompt 使用 `/queue` 定向删除，运行中 prompt 使用 `/interrupt`；连接器回传 `task_cancelled` 及确认状态，云端保护取消请求后的迟到完成/失败事件。连接器心跳现在上报真实 `runningTaskIds`。
- API Vitest 为 5 个文件、25 个用例；Go 测试新增 pending/running 两类取消；TypeScript、Web 生产构建、Go 测试和 Windows amd64 交叉编译均通过。生产 Worker 版本为 `7a9903b1-b7a4-4094-ab31-b4ffd23dc518`，WSL 常驻连接器已更新并保持 active。代码提交 `3c974a4` 已推送 `draft/phase-0`。
- 线上验收发现 Cloudflare D1 免费套餐当日行读取额度已耗尽，实时 API 返回 code 7500，额度将在 UTC 00:00（北京时间次日 08:00）重置。前端此前每 1.5 秒同时拉取预设与最多 100 条完整任务，是明显的读取放大器；已改为预设只加载一次、完整任务按操作刷新，仅有 queued/running 时每 5 秒查询轻量 `/api/tasks/active`。额度重置后需真实验收排队取消、运行中取消、失败重试，并观察 D1 读取量。
