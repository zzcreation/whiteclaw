- `gem_merchant_web` Cloudflare preview 反馈：用户发现长时间不操作可能出现“房间连接已关闭，请重新连接”，已判断需要后续补应用层 heartbeat / pong、客户端自动重连退避、重连后状态恢复校验与服务端离线判定窗口。已把该项加入 `docs/CHECKLIST.md` 里程碑 5。
- 同步加入后台可观测性后续项：房间 Registry、`/admin/stats`、活跃房间数、在线玩家数、最近活动和掉线信息。用户截图显示 Cloudflare Workers Logs 面板提示需要同步 Wrangler 配置；已在 `wrangler.toml` 添加 `[observability] enabled = true` 和 `[observability.logs] enabled = true / invocation_logs = true`，`npx wrangler deploy --dry-run` 通过。
- `gem_merchant_web` 已继续实现连接稳定性和后台统计：协议新增 `room.ping` / `room.pong`，前端每 25 秒 heartbeat，断线后按 1/2/5/10 秒自动重连，并用 resume token 恢复座位；服务端记录玩家 `lastSeenAt` 和房间 `lastActivityAt`。新增 `RoomRegistry` Durable Object 与 `/admin/stats`，返回活跃房间数、在线玩家数、活跃连接数和房间摘要；`wrangler.toml` 新增 `ROOM_REGISTRY` 绑定和 v2 migration。验证：`npm run build`、`npm run lint`、`npm test` 26/26、`npm run test:e2e` 3/3、`npx wrangler deploy --dry-run` 均通过。已本地提交 `e599235 feat: add room heartbeat and stats`，未重新部署 Cloudflare，未推送。
- 用户要求先部署一版并准备后续 UI 设计优化。已执行 `npm run build`、`npx wrangler deploy --dry-run`、`npx wrangler deploy`，线上地址仍为 `https://gem-merchant-web.zzcreation2022.workers.dev`，Cloudflare Version ID `d41e3308-92f1-4551-b601-0267c760508b`。已推送 GitHub `main` 到 `e599235`。线上 smoke：Playwright 两个浏览器上下文加入房间 `LIVE-MREJ6EK1`、准备并开局；页面内请求 `/admin/stats` 返回 `activeRoomCount=1`、`onlinePlayerCount=2`、`activeConnectionCount=2` 且包含该房间。后续 UI 需求：参考用户即将发送的截图，重点优化市场卡需求数字显示、可购买卡高亮、拿宝石选择规则、防止游戏中无关控件占空间、移动端两行常用操作区。
- `gem_merchant_web` 根据用户确认的参考图方向完成一版 UI 交互优化（尚未提交/部署）：房间连接/复制/重开收进右上角“房间”菜单并在操作后自动收起；市场卡成本改为彩色数字圆点；可购买卡高亮区分普通宝石可买（绿色）和需黄金可买（金色虚线），默认支付仍优先普通宝石，高级支付细节收进折叠菜单；拿宝石选择改为最多三色各一，只有单色且供应 >=4 时才可选 2，混色时不会把同色叠到 2；移动端底部行动面板压缩为宝石行 + 动态主行动行，未选卡时只显示拿宝石/跳过。新增截图 `docs/design/mobile-action-redesign.png`。验证：`npm run lint`、`npm run build`、`npm test -- --run`（26/26）、`npm run test:e2e`（3/3）均通过。本地 dev server 运行在 `http://127.0.0.1:5173/`。
- 用户确认“看起来不错，先部署一版”。已提交并推送 `2cc40c7 feat: streamline in-game action UI`，随后执行 `npx wrangler deploy --dry-run` 和正式 `npx wrangler deploy` 成功。线上地址仍为 `https://gem-merchant-web.zzcreation2022.workers.dev`，Cloudflare Version ID `0b177b0e-0ead-4d3c-810b-712484ef6ba6`。线上 smoke：移动端 390px 无横向溢出且底部默认动作仅显示“拿所选宝石/跳过”；房间 `LIVE-MREL1627` 两浏览器加入、准备、开局，SmokeA 拿白/蓝/绿后 SmokeB 侧看到三色各 1 且回合切到 SmokeB。
- 用户指出贵族卡要求也应使用数字显示，并且应改成方形，因为要求的是永久加成/已购卡，不是支付资源。已更新贵族卡要求显示为彩色方形数字块，市场卡成本仍为圆形数字。提交并推送 `938bb5d fix: clarify noble requirements`，`npm run lint`、`npm run build`、`npm test -- --run`（26/26）、`npm run test:e2e`（3/3）均通过；`npx wrangler deploy --dry-run` 和正式部署成功。线上 Cloudflare Version ID `ae3f71e4-0f10-45d3-a879-14e17053c184`，页面检查确认 `.noble-tile .requirement-badge` 存在且移动端无横向溢出。
- 用户提出 UI 细节继续优化：可购买牌绿色边框不明显、预留牌看不到还需要哪些筹码、移动端玩家卡片仍不够紧凑、游戏中不应在玩家操作栏显示取消准备/开始房间。已更新：可买牌高亮改为更粗的绿色/金色内外描边；玩家公开预留卡和“我的预留”列表都显示折扣后的剩余成本数字；移动端玩家卡片压缩到 112px 宽，iPhone 15 Pro 宽度下三张玩家卡完整可见；游戏进行中行动栏移除在线房间准备/开始按钮。验证：`npm run lint`、`npm run build`、`npm test -- --run`（26/26）、`npm run test:e2e`（3/3）通过。提交并推送 `8240426 fix: tighten game UI details`，Cloudflare 部署成功，Version ID `cc3bb358-5f73-4f5f-85de-386a1ec32525`；线上 393px 检查无横向溢出，行动栏仅有宝石按钮和“拿所选宝石/跳过”。
- 用户希望把游戏做成更友好的多人线上游戏平台模式：线上进入先到 Landing Page，包含游戏标题、加入房间、创建房间、选项、About 页面、版本号小标。已实现：默认进入平台首页；加入房间使用昵称/房间码表单；创建房间自动生成 `GM-xxxxxx` 房间并进入在线大厅；选项区包含经典规则和本地试玩；About 为独立页面；版本号显示 `v0.0.0`。E2E 更新为从首页进入本地/在线流程。验证：`npm run lint`、`npm run build`、`npm test -- --run`（26/26）、`npm run test:e2e`（3/3）通过。提交并推送 `6fb9d19 feat: add platform landing page`；Cloudflare dry-run 和部署成功，Version ID `80c6a738-f0fc-4fd8-9111-8aa211795bf9`。线上 smoke：393px 首页无横向溢出，创建房间进入大厅成功；两浏览器从首页加入 `LIVE-MREQZL0D`，准备/开局/拿宝石同步通过。
- 用户要求将上述修改加入总体 checklist，并指出加入房间时会闪一下本地运行界面再进入准备阶段，需要去掉中间本地桌面并加读取 indicator；同时形成 UI 实现规则：所有服务器交互都需要 loading / pending indicator。已更新 `docs/CHECKLIST.md`，补入平台入口、UI 精简、服务器交互读取规则。实现上新增 `loading` screen 和全局 `.server-indicator`：加入/创建房间等待服务器 lobby/snapshot 时只显示 loading，不渲染本地 mock 棋盘；ready/start/在线行动等 sendRoomEvent 会显示通讯 indicator。验证：`npm run lint`、`npm run build`、`npm test -- --run`（26/26）、`npm run test:e2e`（3/3）通过。提交并推送 `4a206c9 fix: add server loading indicators`，Cloudflare Version ID `f1e9438b-a8bf-44e1-96ae-9bce6270ca2b`。线上复测：加入 `LIVE-MREVFTR2` 后 40ms 内 `.loading-page` 存在且 `.market-row` 数量为 0，进入 lobby 后仍无市场桌面；点击准备后 `.server-indicator` 显示“正在提交准备状态...”。

## 2026-07-10 19:39 Memory Flush

- `gem_merchant_web` current online URL: `https://gem-merchant-web.zzcreation2022.workers.dev`.
- Recent deployed commits on `main` after earlier online multiplayer work:
  - `2cc40c7 feat: streamline in-game action UI`: mobile-focused in-game UI redesign. Market card costs use colored circular number chips; purchasable cards highlighted green if buyable without gold and gold/dashed if gold is needed; gem selection and actions compressed into a two-row bottom action area; room/rejoin/copy debug-ish controls moved into right-side room/menu UI.
  - `938bb5d fix: clarify noble requirements`: noble requirements now use colored square number blocks because they represent permanent card bonuses, while market card costs remain circular resource chips.
  - `8240426 fix: tighten game UI details`: stronger purchasable-card highlight; reserved cards show remaining discounted chip cost; player/opponent cards compressed for iPhone 15 Pro width; in-game action bar no longer shows cancel-ready/start-room controls.
  - `6fb9d19 feat: add platform landing page`: online entry now starts at a platform-style Landing Page with game title, join room, create room, options, About page, and version badge `v0.0.0`.
- Latest Cloudflare deployment reported for `gem_merchant_web`: Version ID `80c6a738-f0fc-4fd8-9111-8aa211795bf9` for commit `6fb9d19 feat: add platform landing page`.
- Verification reported for latest deployment: `npm run lint`, `npm run build`, `npm test -- --run` (26/26), `npm run test:e2e` (3/3), `npx wrangler deploy --dry-run`, plus online smoke at 393px mobile width. Smoke covered homepage no horizontal overflow, create room entering online lobby, two browsers joining same room from homepage, ready/start game, and synced gem-taking.
- User’s product direction for `gem_merchant_web`: evolve it from a single game page into a friendly multiplayer online game platform. Landing page requirements confirmed: title, join room, create room, options, About page, small version marker.

## 2026-07-10
- Gem Merchant: handled reserved-card/endgame UI request. Commit b4407f6 adds checklist entries, reserved cards with original costs and mobile purchase access, stronger current-turn prompt, and endgame winner/stat panel. Deployed Cloudflare Version ID 8cddbebc-f58e-43ba-9216-58ffde453cb5. Validated lint/build/unit/e2e and online create/join/start smoke.
