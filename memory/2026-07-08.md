- `gem_merchant_web` 里程碑 3 继续：在已有 Durable Object/WebSocket 房间骨架基础上补断线重连和 Hibernation 支持。`GameRoomController` 增加可持久化 snapshot/hydrate，Durable Object 使用 `state.acceptWebSocket()`、WebSocket attachment 和 storage 恢复连接身份与房间状态；前端保存最近房间 `resumeToken` 到 localStorage，重新加入同一房间时恢复原座位并收到玩家视角 snapshot。新增 controller 重连/持久化恢复测试。验证：`npm run test` 6 文件 20 条通过，`npm run build` 通过，`npm run lint` 通过，`wrangler deploy --dry-run` 通过；`wrangler dev` 双 Node WebSocket smoke test 通过，断开后用 resume token 重连仍为 2 玩家、同一 playerId、`phase=playing`、`version=2`。下一步：host browser + OpenClaw sandbox browser 双浏览器联机截图验收。
- `gem_merchant_web` gating review 修复：用户指出当前改动未通过 `docs/review/2026-07-08-milestone-3-gating-review.md`，已先处理 review 的 Major 1-6。修复内容：预留拿金后超 10 枚进入弃宝石；网络 action 校验非负整数 token/payment/level/slot；resumeToken 改为独立 secret 且不在 lobby 广播；开局 seed 改为服务端 secret；大厅未准备断线玩家释放席位；前端 WebSocket 回调忽略过期 socket。顺带处理部分 Medium/Minor：最终轮 phase 推导、畸形消息关闭 socket、storage rejection 收口、Vite `/api` WS 代理、复制房间码按钮。验证：`npm run test` 6 文件 24 条通过，`npm run build`、`npm run lint`、`wrangler deploy --dry-run` 通过；`wrangler dev` smoke 验证 secret token 重连、公开 playerId 不能劫持、大厅断线席位释放。下一步仍是 host browser + OpenClaw sandbox browser 双浏览器验收。
- `gem_merchant_web` 联机验收记录：sandbox browser 可通过 `http://172.31.254.42:8787` 打开应用、加入房间并准备；曾在 `E2E-714` 与 HostA 成功开局，HostA 视角看到两人、当前轮到 Sandbox、非当前玩家按钮禁用。sandbox 后续行动阶段受子会话状态不复用和本地 `wrangler dev` 掉线影响，未作为完整通过项。改用 host 双浏览器房间 `E2E-HOST` 完成可视化广播复核：HostA 先入房、HostB 后入房，ready/start 后 HostA 拿白/蓝/绿各 1，HostB 视角同步看到 HostA token `1/1/1`、当前玩家 HostB、日志 `HostA took tokens.`；已写入 `docs/review/2026-07-08-milestone-3-gating-review.md`，`docs/CHECKLIST.md` 联机验收标记为 `[review]`。清理了 `wrangler dev` 和 `.wrangler/`；`npm run test` 24 条通过。
- `gem_merchant_web` 测试栈迁移：根据用户 review，先更新 `docs/review/2026-07-08-test-stack-evaluation.md`，明确 OpenClaw sandbox browser 因 `scope=session` 不能跨子会话复用 localStorage/profile，降级为环境冒烟；推荐 Playwright 双 browser context 作为主验收。随后实现下一步：删除旧 `openclaw-sandbox-browser-playwright:test-20260707` Docker 容器/镜像；前端新增在线 lobby roster 状态，开局前不再显示本地 mock 棋盘；补 `data-testid` 断言目标；新增 pinned `wrangler` 与 `@playwright/test`、`dev:worker`、`test:e2e`、`playwright.config.ts`、`tests/e2e/online-room.spec.ts`。E2E 使用双 webServer（worker 8787 + Vite 5173）和唯一房间码，验证 Sandbox 首手拿白/蓝/绿后 HostA 看到 token、银行、回合与日志同步。验证：`npm run lint`、`npm run build`、`npm test` 24/24、`npm run test:e2e` 1/1 通过。Docker 容器/镜像列表清空。
- `gem_merchant_web` 继续修复 Medium 5：前端在线 `run()` 不再乐观显示成功/清空选择；`sendRoomEvent` 返回 actionId，pending map 等待匹配的 `game.actionAccepted` 后才显示成功并清空选择，`game.error` 移除 pending 且保留选择。E2E 增加 `status-message` 断言。更新 CHECKLIST 和 gating review。验证：`npm run lint`、`npm run build`、`npm test`、`npm run test:e2e` 全通过。提交并推送 `8b0be1e fix: wait for online action acknowledgements`。
