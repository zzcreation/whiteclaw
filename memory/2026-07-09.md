- `gem_merchant_web` 继续里程碑 4 体验打磨：实现市场卡购买的手动支付分配 UI。选中市场卡后右侧行动面板显示折后成本、普通宝石支付和金币抵扣的加减步进、自动填充建议、支付状态；购买按钮只有在 paymentPlan 精确覆盖折后成本时可用，前端先拦无效支付，服务端 reducer 仍做最终校验。重置连接/回本地/购买成功会清空选卡和支付计划。更新 `docs/CHECKLIST.md`：市场卡购买支付分配完成，剩余预留卡购买入口、贵族选择、弃宝石弹窗。验证：`npm run build`、`npm run lint`、`npm test` 24/24、`npm run test:e2e` 1/1 通过。提交并推送 `21808fe feat: add market card payment controls`。
- `gem_merchant_web` 继续里程碑 4：补齐预留卡购买入口、贵族选择和弃宝石手动面板。右侧行动面板现在可选择自己的可见预留卡并复用支付分配购买；`awaiting_token_discard` 显示按颜色加减的弃宝石面板，保留自动弃到 10；`awaiting_noble_choice` 显示可选贵族列表并支持点选。新增规则测试覆盖购买预留卡。更新 `docs/CHECKLIST.md`。验证：`npm run build`、`npm run lint`、`npm test` 25/25、`npm run test:e2e` 1/1 通过。提交并推送 `4b6ae0f feat: add reserved and pending choice controls`。
- 用户确认下一步先做移动端布局，并询问能否生成草图 review。已在 `gem_merchant_web` 生成待 review 草图：`docs/mobile-layout-sketch.html` 和渲染截图 `docs/mobile-layout-sketch.png`。草图方案：顶部状态栏、横向玩家条、可纵/横滑公共牌桌、底部行动栏/行动抽屉。尚未提交，等用户确认方向后再正式实现。
- 用户要求参考游戏截图重新设计移动端，先讨论并确定设计稿，不做其他产品代码修改。已将 `gem_merchant_web/docs/mobile-layout-sketch.html` 升级为移动端设计稿 v1，并导出 `docs/mobile-layout-sketch.png`：四屏分别为默认牌桌、拿宝石底部抽屉、购买支付抽屉、查看对手预留卡。仅修改 docs 草图文件，尚未提交，等待用户 review。
- 根据用户 review 调整移动端设计稿：把设计文件移动到 `gem_merchant_web/docs/design/`；默认牌桌进一步压缩 UI 密度，首屏现在能同时看到 III / II / I 三个等级的公共牌。重新导出 `docs/design/mobile-layout-sketch.png`。仍只改设计稿文件，未动产品代码，未提交。
- 用户确认移动端设计稿可以开始实现。已在 `gem_merchant_web` 实现移动端响应式布局：小屏顶部压缩并 sticky、玩家信息横向滑动、贵族/银行/III-II-I 三层公共牌桌紧凑同屏、行动面板改为底部 sticky 抽屉。新增 Playwright 移动端 E2E，断言 390px 视口下 L3/L2/L1 均在视口内且无横向溢出。保留 `docs/design/mobile-layout-sketch.*`，新增实现截图 `docs/design/mobile-implementation.png`。验证：`npm run lint`、`npm run build`、`npm test` 25/25、`npm run test:e2e` 2/2 通过。提交并推送 `72c9aeb feat: add mobile responsive layout`。Vite dev server 运行在 `http://127.0.0.1:5173/`。
- 用户要求继续下一步，已实现错误提示与操作反馈：前端反馈状态从单字符串升级为 `info/success/error/pending` tone，状态条按类型变色并带 `data-status-tone`；在线行动 pending 时禁用关键操作并防重复提交，收到 `game.actionAccepted` / `game.error` 后切换 success/error；连接中、断线、复制房间码、重开本地局等也有明确反馈。修复了本地 `setGame(updater)` 中规则异常无法被 try/catch 捕获导致组件崩溃的问题，改为先计算 next state。新增 E2E 覆盖本地非法操作 error tone 和联机成功 success tone。更新 `docs/CHECKLIST.md`，将错误提示与操作反馈标为完成，动效/音效独立剩余。验证：`npm run lint`、`npm run build`、`npm test` 25/25、`npm run test:e2e` 3/3 通过。提交并推送 `d74a201 feat: improve action feedback`。
- 用户希望动效/音效置后，先部署 Cloudflare 预览用于真机联网测试。已执行 `npx wrangler deploy --dry-run` 通过，随后 `npx wrangler deploy` 成功部署 Worker + Assets + Durable Object 到 `https://gem-merchant-web.zzcreation2022.workers.dev`，Cloudflare Version ID `54135fd4-b45f-419b-8280-d2240ce594d2`。线上 Playwright 双浏览器 smoke test 通过：房间 `LIVE-MRD27HIU` 中 PhoneA/PhoneB 加入、准备、开局，PhoneA 拿白/蓝/绿后 PhoneB 看到同步。更新 README 和 `docs/CHECKLIST.md` 记录预览地址并标记 Cloudflare preview 完成；提交并推送 `6c4775f docs: record cloudflare preview`。
