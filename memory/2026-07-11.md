- `gem_merchant_web`：按用户要求为微信分享补充页面 meta。`index.html` 新增 description、itemprop、Open Graph、Twitter large image 标签，指向线上域名 `https://gem-merchant-web.zzcreation2022.workers.dev/` 和 `/share-card.png`；`public/` 新增 `share-card.svg` 与导出的 1200x630 `share-card.png`。验证：`npm run build` 通过。尚未部署到 Cloudflare；部署后如微信仍显示旧信息，可能需要等待微信分享缓存刷新。
- 用户在 Cloudflare 添加 custom domain `https://gem-merchant-web.hezzze.com` 用于规避 DNS 污染；已将所有分享 meta URL 改为该域名，`npm run build`、`npx wrangler deploy --dry-run`、`npx wrangler deploy` 均通过。Cloudflare Version ID `5e30852d-884e-45d5-9a77-9cbd58133fa8`。线上验证：`https://gem-merchant-web.hezzze.com/` 返回 200 且 head 中 `og:url` / `og:image` 为 hezzze.com，`/share-card.png` 返回 200 image/png。提交并推送 `625f607 feat: add social sharing metadata`。
