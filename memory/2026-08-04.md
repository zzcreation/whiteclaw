# 2026-08-04

- `zzc-cloud-server` 私有仓库：`https://gitlab.zzcreation.com/zzc2/zzc-cloud-server`，开发分支 `dev`，提交后由 GitLab CI 自动部署 Sandbox。
- 新建前端项目 `projects/zzc/zzc-sl-admin`，技术栈 Refine + Ant Design + React + Vite；当前已完成登录、JWT 刷新、玩家列表和详情，本地提交 `e28aa83`。
- 仓库归属已调整：`zzc-cloud-server` 继续以私有化 GitLab 为代码基线；`zzc-sl-admin` 改以私有 GitHub 仓库 `https://github.com/zzcreation/zzc-sl-admin` 为代码基线和 CI 平台。
- 前端部署 Cloudflare Pages 前仍需 HTTPS API 域名与 CORS 白名单，当前 Sandbox API 为 HTTP。
- `zzc-sl-admin` 已加入 Vitest + Testing Library + MSW 集成测试和 Playwright E2E。GitLab CI 曾因 Runner 拉取外部 Node 镜像 `unexpected EOF` 失败，后来虽通过本地专用镜像解决，但考虑 ECS 稳定性与存储，前端已删除 `.gitlab-ci.yml`、`ci/Dockerfile` 和系统 Chromium 适配，迁移到 GitHub Actions（提交 `9a0cb09`、`86967f4`）；首条无警告流水线 `30924459990` 的 verify 与 E2E 均成功。
- `zzc-sl-admin` 的产品约束已确认：使用 Refine + Ant Design；首版不区分管理员角色；GM 可选择任意 `VideoSequenceConfig`，真实游客链路仅使用 active 配置；一个 interaction point 就是一台设备，即使同一主机有两个交互点也按两个设备建档，以全局唯一 SN 区分。PRD 和后端接口计划位于 `projects/zzc/zzc-sl-admin/docs/`。
- `zzc-cloud-server` 已完成前端阻塞型后端 Batch 1–4：GM 玩家分页/详情聚合与统一错误结构；GM 可按任意 configId 生成影片并保存配置快照；视频素材列表、鉴权预览/下载、上传覆盖、删除和配置完整性检查；设备档案扩展及存量数据库增量升级。对应关键提交为 `f2c0571`、`b67bc73`、`190dbc5`、`74fbcb7`、`4892606`，GitLab 流水线 #648–#651 均通过并部署 Sandbox。
- `zzc-cloud-server` 第一阶段 BUG-01～08 已清完：统一计分、刷卡不写 0 分、成绩 1–4 校验、按展项聚合并以全部配置展项为评级分母、影片 `_Z`/选项/默认片回退、空库建表与 refresh token 30 天秒制。关键提交 `70952fc`、`718a9bf`、`4a3a25f`、`7cbc6ef`。
- `zzc-sl-admin` 已完成游戏组、选择组和设备档案 CRUD，提交 `28a3d66`。设备表单包含 SN、类型/分组、楼层/分区/位置、三方与健康地址、超时、共享密钥、备注和启停状态；SN 与设备类型创建后锁定。
- 前端自动化当前基线：集成测试覆盖登录成功/失败、JWT 自动续签重放、玩家列表 API 与页面；Playwright E2E 覆盖登录、玩家列表、新建游戏组、注册设备。测试曾发现设备弹窗默认值未挂载导致首次注册无法提交，已修复并纳入回归。
- `zzc-sl-admin` 已完成交互模拟器第一批（提交 `76395f0`）：可选玩家与启用设备，游戏点支持 1–4 分或只计次，选择点提交设备选项；逐次二次确认、写入后刷新玩家快照并记录本次时间线；可选择任意 VideoSequenceConfig 生成影片并每 2 秒轮询任务，展示结果或失败原因。集成测试增至 6 条，E2E 扩展为登录、维护展项、模拟 4 分、指定配置生成及轮询成片；GitHub Actions `30927425784` 全绿。
