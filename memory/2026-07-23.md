# 2026-07-23

- `projects/zzc/pass_doudou` 将断线生命周期提交及后续文档/测试提交推送到 `origin/main`；最新提交为 `6d71ce6 test: support deployed environment smoke checks`，工作树已同步。
- Cloudflare 对实现 Durable Object 的 Worker 不生成 Version Preview URL，因此未切换 `pass-doudou` 生产流量，改为部署独立预览 Worker：`https://pass-doudou-preview.zzcreation2022.workers.dev`，版本 `7fd34fd9-71b4-40bf-840d-9d0eab008f99`。
- Playwright 支持通过 `PLAYWRIGHT_BASE_URL` 直接验收已部署环境；远端完整 E2E 曾暴露用例竞态（领出方未确认收到桌面牌便判断 PASS 后空桌），已改为每个动作等待双方状态同步。
- 最终验证通过：Vitest 77/77、生产构建、本地 Playwright 3/3、远端 Preview Playwright 3/3、Wrangler dry-run。`docs/CHECKLIST.md` 已勾选 Cloudflare Preview 部署。
- `pass_doudou` 完成 M3 第一阶段手机端体验：玩家状态横向状态条、当前争夺优先、手牌横向多选、选中计数、合法/非法选择视觉反馈、手机端粘性操作区；对应提交 `bdcc730 feat: improve mobile turn flow`。
- 移动端 E2E 现验证 390×844 视口下 5 人状态横滑、手牌滚动、两张牌多选、粘性操作区和页面无横向溢出。Preview 更新为版本 `844201f5-eeff-4329-b42f-880be3ff4a1f`，线上 E2E 3/3 通过。
- Preview 公网/代理链路偶发 WebSocket 握手或状态回传抖动；远端 Playwright 断言预算调整为 15 秒并允许 1 次重试，本地仍保持 5 秒、零重试，提交 `f861672 test: tolerate preview network jitter`。
- `pass_doudou` 完成 M3 第二阶段，提交 `5c15405 feat: explain flip and augment previews`：本地与在线 UI 共用中文牌型/错误文案，翻牌按钮明确显示升级/还原目标，牌面标记基础面/升级面，预览拆分翻牌、自动增强与合计 token，并解释 token 不足或非法压制原因。
- 新增 UI 预览单测与翻牌 E2E，验证总数更新为 Vitest 81/81、Playwright 4/4；M3 的翻牌预览、增强预览、出牌合法性提示已在 checklist 勾选。
- Preview 更新为版本 `5c793ddc-f1d5-48d5-8a67-71c0fd64dd21`。首次线上 E2E 命中尚未传播的新旧混合静态资源并由重试通过，待边缘传播后整套复跑 4/4 无重试通过。
- `pass_doudou` 完成 M3 最后一项移动端视觉回归：新增 390×844 确定性 5 人牌局，在翻到升级面后对完整回合面板执行 Playwright 像素截图比对，基线为 `tests/e2e/local-game.spec.ts-snapshots/mobile-turn-flip-preview-chromium-linux.png`。提交 `7056047 test: add mobile visual regression baseline`。
- 初版基线在开发与生产构建间因 React StrictMode 开发模式额外初始化牌局而出现内容差异；未放宽像素阈值，改为页面加载后、显式新建牌局前重置随机种子，提交 `778399a test: stabilize mobile baseline across builds`。本地与线上最终均为 Playwright 5/5，截图像素比对通过。
- Preview 最终版本 `06af4c08-06f7-4541-9d1b-aaa767a96863`，地址仍为 `https://pass-doudou-preview.zzcreation2022.workers.dev`。视觉基线图已发送至当前飞书单聊供用户 review。
