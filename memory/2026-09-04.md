# 2026-09-04

## zzc-comfy-cloud durable project state

- Canonical project path after OpenClaw 2026.8.2 workspace migration: `/home/zzc/.openclaw/workspace/main/projects/zzc/zzc-comfy-cloud`.
- The former root workspace was migrated into `workspace/main/`, including `projects/`, memory, skills, artifacts, hidden directories, and the original workspace `.git`. The project and workspace Git history were preserved. A backup of the 8.2-generated empty/default `main` workspace is under `~/.openclaw/backups/workspace-main-migration-20260903-1308/preexisting-main/`.
- OpenClaw config now explicitly points `main.workspace` and `browser-sandbox.workspace` to `~/.openclaw/workspace/main`. Memory index was rebuilt successfully after migration.
- Project branch: `draft/phase-0`. Root progress file: `backlog.md`.
- Production site/API: `https://ai-tool.hezzze.com` (Cloudflare Worker, custom domain). Health endpoint was healthy after the latest deployment.
- Cloudflare resources: D1 database `zzc-comfy-cloud`; R2 bucket `zzc-comfy-cloud-assets` remains for workflow JSON and historical assets. D1 free-tier row-read quota was exhausted late 2026-09-03; it resets at UTC 00:00 / Asia-Shanghai 08:00. Frontend polling was optimized to prevent recurrence: presets load once; full tasks refresh on action; lightweight active-task polling every 5 seconds only while active tasks exist.
- Cloudflare Gateway environment rule discovered: systemd injects managed values from `~/.openclaw/gateway.systemd.env` before OpenClaw reads `~/.openclaw/.env`; dotenv never overrides an existing process variable. `gateway restart` alone does not regenerate the snapshot. Safe refresh command used: `env -u CLOUDFLARE_API_TOKEN -u CLOUDFLARE_ACCOUNT_ID openclaw gateway install --force`, then restart. Never expose credentials; compare fingerprints only.

## Storage and workflows

- Production storage provider is now Qiniu. Non-secret config:
  - bucket: `zzc-mini`
  - base path: `zzc-comfy-cloud/prod`
  - private bucket: true
  - CDN: `https://cdn.zzcreation.com`
  - upload endpoint: `https://up-z1.qiniup.com` (South China / z1)
- Qiniu AK/SK exist as Worker secrets; do not read, echo, or put them in files/chat.
- Qiniu upload signing bug fixed: URL-safe Base64 padding `=` must be preserved for upload policies and HMAC-SHA1 signatures. Private CDN signing was fixed at the same time.
- Qiniu end-to-end validation passed: browser input PNG uploaded under `zzc-comfy-cloud/prod/tmp/...`, ComfyUI consumed it, MiniMax H3 generated an MP4, connector uploaded it under `zzc-comfy-cloud/prod/tasks/...`, object became ready, and private CDN preview worked.
- Six workflow presets are deployed. `workflow_samples/minimax-h3.json` is integrated as MiniMax H3 v2.
- MiniMax H3 aspect-ratio bug fixed: ComfyUI `ResolutionSelector` accepts `16:9 (Widescreen)`, not `16:9 (Landscape)`. All 8 live enum values now match `.229` `/object_info`, and the UI renders options dynamically from the preset.
- Generated files currently remain both in the ComfyUI output folder on the Windows GPU host and in Qiniu. The connector does not delete local ComfyUI outputs. Historical R2 objects remain readable.

## Hosts and connector target

- Main OpenClaw Windows host / current WSL host: `192.168.130.29`.
- ComfyUI RTX 3090 Windows host: `192.168.130.229:8188`. These are different machines.
- Product requirement: final connector is a Go single-file `connector.exe`, installed by default on the same Windows host as ComfyUI (`.229`) and run as a Windows Service. Remote-LAN mode is configurable but not the Phase 0 default/acceptance target.
- Current production bridge is still a persistent systemd user service in the `.29` WSL: `zzc-comfy-cloud-connector.service`, enabled and active, with proxy configuration and automatic restart. It is temporary and must be removed only after the native Windows Service on `.229` passes real task/reboot/network/ComfyUI-restart acceptance.
- Windows connector CLI/service skeleton and amd64 cross-build work are in progress; service install, ACL, logs, proxy, recovery policy, and real `.229` install remain.
- Qiniu protocol mismatch fixed in the connector: cloud returns nested `upload.url`; an older WSL binary only read top-level `uploadUrl` and attempted `PUT ""`. The service binary was rebuilt/replaced, protocol regression test added, and heartbeat recovered.

## Product features shipped

- Frontend supports input previews, task thumbnails, image/video detail previews, and original-file links.
- Admin can create ordinary accounts from “管理后台 → 用户”; creation is fixed to role `user`, includes Chinese form, display name, password confirmation/minimum length, duplicate-email feedback, account list, responsive layout.
- Ordinary users see only AI creation and their own tasks; admin UI is hidden and direct admin API access returns 403. Forging `role=admin` in create-user requests is rejected.
- Task operation loop shipped:
  - cancel queued tasks;
  - request cancellation for running tasks through API → ConnectorSession → connector → ComfyUI (`/queue` delete for pending prompts, `/interrupt` for running prompts);
  - retry failed/cancelled/dispatch-failed tasks as a new task using the original workflow version and parameters;
  - Chinese error summary/advice with raw technical details collapsed.
- Connector heartbeats now report the actual running task ID.

## Recent commits and verification

- Notable pushed commits in order: `d6f2e29` (MiniMax ratio v2), `62c6b9d` (Qiniu non-secret config), `48d1868` (Qiniu provider switch), `64dc9d2` (Qiniu Base64 padding), `a30edb1` (z1 upload endpoint), `1b73fe1` (connector nested Qiniu upload protocol), `60b57b3` (Qiniu E2E acceptance/backlog), `fa92258` (ordinary accounts), `3c974a4` (task cancel/retry/error UX and polling optimization).
- Latest task-operation build passed TypeScript, Web production build, API Vitest 25/25, Go tests, and Windows amd64 cross-compilation.
- Latest known Worker version after task operations: `7a9903b1-b7a4-4094-ab31-b4ffd23dc518`.

## Next priorities

1. After D1 quota reset, run real production acceptance for queued cancel, running cancel, and failed-task retry.
2. Continue frontend-core validation across the other five workflows and improve stage/progress/error UX as needed.
3. Finish/install the native Windows connector service on `192.168.130.229`; validate reboot, disconnect/reconnect, ComfyUI restart, proxy, logs, ACL, and recovery, then retire the WSL bridge.
4. Add reconnect reconciliation, exponential backoff, real running-job heartbeat, capability matching, stronger SSRF protection, and generated protocol types.
5. Add Qiniu temporary/failed-object cleanup and retention; keep R2 until workflow JSON and historical-object dependencies are deliberately migrated.
6. Account enhancements still missing: user self-service password change and admin password reset.

## 2026-09-04 09:24 — zzc-comfy-cloud durable status

- Canonical project path after OpenClaw 2026.8.2 workspace migration: `/home/zzc/.openclaw/workspace/main/projects/zzc/zzc-comfy-cloud`. The old workspace Git history was migrated to `workspace/main/.git`; project branch is `draft/phase-0`. Use the absolute project path for future work.
- Production site/API: `https://ai-tool.hezzze.com`; Cloudflare Worker/D1 is healthy. The D1 free-tier daily row-read quota was exhausted on 2026-09-03 due to an old 1.5s full-list polling loop, then reset at 2026-09-04 08:00 CST. Login and real row reads were verified recovered. Frontend polling was changed so presets load once and only active tasks use lightweight 5s status polling.
- Production storage provider is Qiniu. Config: bucket `zzc-mini`, base path `zzc-comfy-cloud/prod`, private CDN `https://cdn.zzcreation.com`, upload endpoint `https://up-z1.qiniup.com` (South China). Qiniu AK/SK are Worker secrets and must never be echoed. Input assets and generated outputs have passed the end-to-end Qiniu flow. R2 remains available for historical assets and Workflow JSON.
- MiniMax H3 is integrated as preset v2. Its aspect ratio values now exactly match the ComfyUI `ResolutionSelector` enum, including `16:9 (Widescreen)`. A real MiniMax task succeeded with input PNG and output MP4 stored in Qiniu.
- Current compute bridge is still a persistent systemd user service in the main OpenClaw WSL on Windows host `192.168.130.29`, connecting to ComfyUI on the separate RTX 3090 Windows host `192.168.130.229:8188`. This is a temporary bridge. The formal deliverable remains a Go single-file Windows `connector.exe` installed as a Windows Service on the ComfyUI host `.229`; do not mistake the WSL service for final delivery.
- Account management is live: admins can create ordinary `user` accounts; the endpoint cannot create admins even if `role=admin` is forged. Ordinary users only see AI creation and their own tasks/assets, and admin APIs return 403.
- Task UX/API is live: queued/running cancellation, retry for failed/cancelled tasks, readable Chinese error summaries with technical details, and connector-side ComfyUI `/queue` deletion or `/interrupt`. Latest relevant commit for this feature: `3c974a4`; API tests 25/25, TypeScript, web build, Go tests, and Windows amd64 cross-build passed.
- Immediate next validation: perform real UI tests for queued cancel, running cancel, and failed retry now that D1 quota has reset. Then prioritize installing the native Windows connector on `.229`, verify service restart/network loss/ComfyUI restart recovery, and retire the WSL bridge. Further backlog: reconnect reconciliation, exponential backoff, running-task heartbeats, capability matching, Qiniu cleanup policy, and remaining Workflow/UI validation.

## 2026-09-04 09:39 — D1 query-index optimization

- Added and remotely applied `0003_task_query_indexes.sql`: a partial index for per-user queued/running tasks and a composite index for task asset preview/count/detail lookups. The historical `0002_asset_storage.sql` schema was already present but missing from Wrangler's `d1_migrations` ledger; its columns/index/data were verified before safely recording it as applied, then `0003` applied normally.
- Production `rows_read` measurements on the current dataset: active-task query reduced from 13 to 2 rows; full task list reduced from 607 to 47 rows. A window-aggregation rewrite measured 141 rows after indexing and was deliberately not used because the indexed correlated query was cheaper.
- Root `npm test` remains green at 25/25 API tests; Health is OK. Backlog updated, D1 usage alert deferred per user request, and commit `bbe52f3` pushed to `draft/phase-0`.

## 2026-09-04 10:43 — production release policy

- The site now has real external users. Treat production as continuity-sensitive, not as an interruptible spike environment.
- Before production changes, verify scope, tests/build, health, connector heartbeat, queued/running tasks, migration state, and rollback target. Keep D1 and Worker/connector protocol changes backward compatible; do not restart the connector while jobs are running. Any downtime, destructive schema/data change, auth/storage switch, or other irreversible/high-risk operation requires explicit user confirmation first.

## 2026-09-04 11:18 — MiniMax H3 first/last-frame v3

- Live ComfyUI `.229` object info confirms `MiniMaxH3ImageToVideo` supports optional native `first_frame` and `last_frame` IMAGE inputs. MiniMax H3 v3 was published as an immutable workflow version: `firstFrame` is required, `lastFrame` is optional. When no last frame is submitted, the scheduler removes the static `last_frame` connection before dispatch so v3 preserves first-frame-only behavior.
- The creation UI now supports multiple `asset_ref` fields with separate upload, preview, removal, required/optional badges, and responsive mobile layout. A missing viewport meta tag was fixed so mobile breakpoints actually apply.
- Task creation and scheduler materialization now require every input asset to be `ready` and owned by the task user, closing cross-user asset-ID injection now that ordinary users are live.
- Production rollout followed the new guardrails: tests/build passed (API 33/33), no queued/running tasks, connector online and idle, no D1 migration or connector restart. Worker version `dc1bfcac-165d-4c64-88c8-737c81fd7bfe` deployed; prior rollback version is `7a9903b1-b7a4-4094-ab31-b4ffd23dc518`. MiniMax current version is v3 ID `c5fbf578-e488-4fd1-8512-7e0321982957`; v2 ID `a8d8f191-dd06-4d2a-8fd4-37fffd6f5817` remains available for rollback. R2 workflow SHA-256 verified as `0160fe89b327c9785701fecc71ab4d918f02561c8df303ff9964d2a10b46dbe2`.
- Post-deploy health/home/D1/connector checks passed and no task was interrupted. Real E2E acceptance remains: one first-frame-only task and one first+last-frame task, verifying both Qiniu input objects, ComfyUI consumption, output upload, and private CDN preview. Code commit `e48fa43` pushed to `draft/phase-0`.

## 2026-09-04 13:22 — Wan I2V compatibility and empty-success fix

- Wan I2V v1 failed because `.229` lacked `JjkText`; full live `/object_info` comparison also found `JC_GGUF_adv`, `DF_Image_scale_to_side`, and `DF_Get_image_size` missing. Wan I2V v2 replaces these with installed equivalents: `TextInput_` feeds the user prompt directly, while `JWImageResizeByLongerSide` plus `GetImageSize` preserves the 640px-long-side image preprocessing. All 21 resulting node types and relevant enum values match the live ComfyUI installation.
- Wan I2V v2 ID is `2f09b29b-9650-43bd-824a-326dc6c6f379`; R2 workflow SHA-256 is `bfc6a29ca91871acae52068abb187c230dbf4902e1aa026f75718e463888ec19`. v1 remains available for rollback. Users must create a new task; retrying the old failure deliberately retains v1.
- Fixed the ComfyUI partial-execution “empty success” bug at two layers. Connector now parses HTTP-200 `node_errors`, detects `execution_error` messages regardless of `status_str`, and fails completed histories with zero supported image/video/text outputs. Cloud ConnectorSession independently turns legacy `task_completed` messages with empty `uploadIds` into a failed task; frontend shows a readable no-output explanation.
- Production rollout respected live-user guardrails: tests passed (API Vitest 39/39, TypeScript, web build, Go tests, Windows amd64 cross-build), no queued/running tasks, Worker deployed first as compatible guard (`c6fb219d-0643-4e6a-9340-9732c323dc56`), then the idle WSL connector was upgraded and reconnected. Previous connector binary is preserved as `connector.pre-empty-success`; prior Worker rollback is `dc1bfcac-165d-4c64-88c8-737c81fd7bfe`.
- Commits were intentionally split and pushed: `c149778` (Wan I2V workflow), `b9e54a5` (connector detection), `9de3296` (cloud fallback), `ba26b3d` (backlog). Real acceptance still requires a new Wan I2V v2 task after the user added the missing models.
