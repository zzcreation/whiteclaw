- `projects/zzc/gem-merchant-web` 的林语公会（Grove Guild）simple-card 三级样牌已全部完成并经 M2 审批：L2 采用“抬手鉴定翡翠、叉腰、非对称长耳、分尾工匠服”；L3 最终修正版采用自然下垂双耳、翡翠权杖与大师披风，用户明确回复“L2 通过”“L3通过”。L2/L3 均已从 staging 晋级为正式 master/runtime 资产，并完成透明度、透明四角、安全边距及 72/96px 小尺寸可读性检查；未改动其他公会，也未启动 90 张量产。
- Grove Guild L3 正式资产：`art/masters/grove-guild-l3-001_simple_master.png`（1254×1254 RGBA）和 `src/assets/cards-simple/grove-guild-l3-001.webp`（256×256 无损透明 WebP）。母版与批准 cutout 的 SHA-256 一致，runtime 与母版的 256px Lanczos 缩放像素一致；staging 的 asset spec、provenance、QA 报告和资产台账已记录 M2 批准。
- OpenClaw 使用 OpenAI OAuth + `openai/gpt-image-2` 生图时，偶发 `chatgpt.com:443` 连接/TLS 超时并非 OAuth 或模型配置错误。该路径走 Codex Responses / `chatgpt.com`，近期上游图像服务也存在公开的网络错误与延迟波动；本机经 `172.31.240.1:7897` 代理实测亦有 TLS 握手超时，直连不可用，因此主要稳定性风险是上游链路与代理出口叠加。
- 已为上述生图稳定性问题落地防护：OpenClaw 生图超时提高到 600 秒；关闭自动跨供应商回退，保持 OpenAI OAuth + GPT-image-2 风格锁定；项目新增 `tools/openclaw-image-retry.sh`，最多 3 次退避，仅重试“连接尚未建立”的安全网络错误，避免对可能已提交成功的泛化超时/5xx 重试造成重复生图或扣费；`docs/art-pipeline.md` 已同步。若仍出现握手失败，应优先切换 Windows 侧 7897 后的代理节点或增加多出口健康切换。
- 角色生图 QA 新增明确经验：正式抠图/上评审页前先计数耳朵与四肢。L3 初稿曾生成三只耳朵而判废；后续耳朵提示词应强调“两耳都受重力自然下垂，耳尖朝下”，避免 `sweeps upward/backward`、`banner-like arc` 等表述导致耳朵越过头顶或像旗帜上扬。
