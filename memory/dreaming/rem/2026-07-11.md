# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- `gem_merchant_web` 继续修复 Medium 5：前端在线 `run()` 不再乐观显示成功/清空选择；`sendRoomEvent` 返回 actionId，pending map 等待匹配的 `game.actionAccepted` 后才显示成功并清空选择，`game.error` 移除 pending 且保留选择。E2E 增加 `status-message` 断言。更新 CHECKLIST 和 gating review。验证：`npm run lint`、`npm run build`、`npm test`、`npm run [confidence=0.58 evidence=memory/2026-07-08.md:5-5]
- `gem_merchant_web` 继续里程碑 4 体验打磨：实现市场卡购买的手动支付分配 UI。选中市场卡后右侧行动面板显示折后成本、普通宝石支付和金币抵扣的加减步进、自动填充建议、支付状态；购买按钮只有在 paymentPlan 精确覆盖折后成本时可用，前端先拦无效支付，服务端 reducer 仍做最终校验。重置连接/回本地/购买成功会清空选卡和支付计划。更新 `docs/CHECKLIST.md`：市场卡购买支付分配完成，剩余预留卡购买入口、贵族选择、弃宝石弹窗。验证：`npm run build`、`npm run lint`、`npm tes [confidence=0.58 evidence=memory/2026-07-09.md:1-4]
- `gem_merchant_web` 里程碑 3 继续：在已有 Durable Object/WebSocket 房间骨架基础上补断线重连和 Hibernation 支持。`GameRoomController` 增加可持久化 snapshot/hydrate，Durable Object 使用 `state.acceptWebSocket()`、WebSocket attachment 和 storage 恢复连接身份与房间状态；前端保存最近房间 `resumeToken` 到 localStorage，重新加入同一房间时恢复原座位并收到玩家视角 sn [confidence=0.58 evidence=memory/2026-07-08.md:1-4]
