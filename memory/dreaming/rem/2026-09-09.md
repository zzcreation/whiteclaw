# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- - 用户当前要求先讨论计划，尚未授权开始部署。 - 后续拟在 `.229` 测试真实 Windows x64 `connector.exe` / Windows Service： 1. 发布前只读预检云端与 ComfyUI 队列、本机 ComfyUI 地址、出站网络/代理、磁盘及既有服务。 2. 从 v0.1.1 构建 Windows amd64 EXE并校验 SHA-256，目标目录 `C:\\ProgramData\\ZZCComfyCloud`；凭据仅通过受保护配置文件迁移并限制 ACL，不在聊天、命令行或日志暴露。 3. 保持 WSL Connector 在线，先运行 Windows EXE 的 `version` 与 `doctor`，验证访问 `127.0.0.1:8188` 和云端。 4. 确认空队列后停止 WSL 临时 Connector，再安装/启动 `ZZCComfyCloudConnector` Windows Service；禁止同身份双 Connector 并行争抢任务。 5. 验收云端心跳、Windows/v0.1.1 版本、服务稳定性、队列一致性，并跑一条低成本 `Qwen Image Fast` 真实任务，验证领取、执行、七牛回传、终态落库与前端预览。 6. ComfyUI/整机重启恢复测试属于有影响操作，需用户另行确认窗口。 7. 回滚策略：停止 Windows Service、恢复 WSL Connector；完整验收前只停用、不删除 WSL 服务和旧二进制。 [confidence=0.69 evidence=memory/2026-09-08.md:48-57]
- - 空队列切换已完成：WSL 临时 Connector 停止并禁用自启动但未删除；`ZZCComfyCloudConnector` Windows Service 已安装并为 Running/Automatic。 - Windows Connector 已成功执行 MiniMax H3 Text to Video、Wan 2.2 Text to Video 与用户提交的 Qwen Image Fast；Qwen PNG 在七牛为 ready，领取、ComfyUI 执行、上传与终态闭环通过。 - v0.1.2 Worker 部署引发一次 WebSocket `1006 unexpected EOF`，Windows Connector 自动重连并在数秒内恢复心跳，未产生重复任务或状态不一致。 - 仍待单独窗口验证 Windows Service/ComfyUI/整机重启、断网和 Cloudflare 短暂不可达恢复；在此之前保留禁用的 WSL unit 和旧二进制作为人工回滚路径。 ## v0.1.2 prompt placeholder 修复 - 用户截图发现 Qwen Image Fast 错误显示 R2V 的 `<Picture 1>/<Video 1>/<Audio 1>` placeholder；根因是 textarea 文案硬编码。 - 修复后文生图、文生视频、图生视频显示各自场景化提示；只有包含参考素材集合的预设才按实际 asset kind 显示参考标签。 - 新增 Web 首组 3 个 Vitest 策略用例；API 61/61、Web 3/3、TypeScr [confidence=0.63 evidence=memory/2026-09-08.md:50-58]
- - Wan I2V v1 failed because `.229` lacked `JjkText`; full live `/object_info` comparison also found `JC_GGUF_adv`, `DF_Image_scale_to_side`, and `DF_Get_image_size` missing. Wan I2V v2 replaces these with installed equivalents: `TextInput_` feeds the user prompt directly, while `JWImageResizeByLongerSide` plus `GetImageSize` preserves the 640px-long-side image preprocessing. All 21 resulting node types and relevant enum values match the live ComfyUI installation. - Wan I2V v2 ID is `2f09b29b-9650-43bd-824a-326dc6c6f379`; R2 workflow SHA-256 is `bfc6a29ca91871acae52068abb187c230dbf4902e1aa026f75718e463888ec19`. v1 remains available for rollback. Users must create a new task; retrying the old [confidence=0.55 evidence=memory/2026-09-04.md:96-98]
