# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- OpenClaw browser 官方文档结论：非 sandbox 主会话默认 `target="host"`，sandboxed session 默认 `target="sandbox"`。Host browser 跑在宿主机 OpenClaw-managed Chrome/CDP 上，适合本地 dev server 验证但需要 SSRF allowlist；sandbox browser 跑在 Docker Chromium/noVNC/CDP 容器内，隔离更强但依赖 sandboxed runtime、Docker 镜像、网络和 CDP bridg [confidence=0.65 evidence=memory/2026-07-06.md:60-63]
- `gem_merchant_web` 2026-07-06 16:23 记忆刷新：当前持久记录中最后一个已确认并推送的提交是 `b207053 test: add full game simulation`，`docs/CHECKLIST.md` 显示里程碑 1 已完成，下一步为里程碑 2 本地多人 UI。`src/App.tsx` 当前仍是静态桌面原型数据/布局，尚未从文件内容确认 reducer 已接入 UI。 [confidence=0.58 evidence=memory/2026-07-06.md:11-11]
- `gem_merchant_web` 2026-07-06 18:23 记忆刷新（续）：里程碑 2 已完成并推送 `cada5c5 feat: wire local mock game UI`。`src/App.tsx` 已接入真实 reducer + `createClientGameView`，本地多人 mock 游戏可玩。下一步是里程碑 3（在线联机 / Cloudflare Durable Objects）。; OpenClaw sandbox browser 配置：在 `openclaw.json` 中已添加 `browser-sandbox` [confidence=0.58 evidence=memory/2026-07-06.md:25-28]
