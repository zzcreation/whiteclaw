# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- # 2026-08-07 - `zzc-cloud-server` 增加 DI20 单字节 ASCII `0`（`0x30`）心跳兼容，同时保留 `0x00`；单测、Sandbox Golden、PRD/Backlog/Checklist 已同步。提交 `b42c5e0` 推送到 `dev`，GitLab Pipeline #700 最终全绿。 - Sandbox Golden 首次在既有视频重试等待窗口停留于 `PROCESSING`，重跑成功；本批新增的 `0x00` / `0x30` 真实 TCP 无 ACK 验证两次均通过。 - `zzc-sl-admin` 手测 README 将公网 DI20 端口从 `3105` 修正为实际 `3205`，并补两种心跳说明。提交 `e73a155` 推送到 `main`，GitHub Actions run `31140420907` 重跑后全绿，Cloudflare 部署与认证 Sandbox smoke 成功。 - 从公网实测 `39.106.154.140:3205`：`0x00` 与 `0x30` 均在 1 秒内无 ACK、连接未关闭；Sandbox API health 与管理后台均 HTTP 200。 - 七牛手测前置核对：项目 CI 已配置并掩码保存 `SANDBOX_QINIU_ACCESS_KEY` / `SANDBOX_QINIU_SECRET_KEY`，另有 Bucket/上传前缀变量；但 `deploy/compose.sandbox.yml` 仍固定关闭云上传并使用 `sandbox.invalid`，真 [confidence=0.65 evidence=memory/2026-08-07.md:1-7]
- # 2026-08-17 - `zzc-cloud-server` 在 Sandbox 完成一次真实 DI20 读卡器 + 实体卡 Golden 全流程。读卡器 `DI20260803000002` 读取原始 UID `20B30E14`，业务十进制 UID `0336507680`；真实 TCP 地址仍为 `39.106.154.140:3205`。未激活卡、未注册设备和 CLOSED 卡都返回 `-1`；成功响应精确为 `DI20_Y_V21_SUC`。现场与 DI20 手册一致：成功为一声短鸣，通用失败为两声并亮红灯，超时为三声。 - Golden 正向链路通过：实体卡激活后，真实读卡器触发 vendor-stub `ACCEPTED` 并创建 ACTIVE game session；V2 成绩回调首次 `200 PROCESSED`，相同 payload 重放 `200 ALREADY_PROCESSED`，同 session 不同成绩 `409 ERROR`。最终玩家游戏次数 2、总分 4、评级 A。 - Checkout/出片链路通过：两个并发 checkout 收敛到同一 `jobId/resultUrl`，手环进入 `CLOSED`，覆盖率快照 1/1、成绩 4、评级 A；FFmpeg 成功合成 `manual_game_A.mp4`，稳定结果地址正确返回 HTTP 303。Sandbox 当前关闭七牛上传，目标仍为 `sandbox.invalid`，因此本轮只验收本机合成与稳定 URL，未验收真实七牛上传。 [confidence=0.64 evidence=memory/2026-08-17.md:1-5]
- - 后端 RFID P0 已在本地完成但尚未提交、尚未部署：校验顺序改为先查 SN/点位、后查手环；增加类型化拒绝结果（含 `DEVICE_NOT_REGISTERED`、未激活、CLOSED、供应商主机缺失、MANUAL、供应商启动失败）；预期拒绝仅由 Handler 记录一次结构化 WARN，内部故障仅记录一次 ERROR 且保留 cause；设备协议保持成功 ACK/失败 `-1` 不变。新增 10 个测试后全量为 109 tests，0 failure/error/skipped。下一步是部署前检查、提交/推送并通过 CI 部署 Sandbox，再用新卡复验未知 SN 优先、注册后成功、CLOSED 拒绝和单条日志。 [confidence=0.62 evidence=memory/2026-08-17.md:10-11]
