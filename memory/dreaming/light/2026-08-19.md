# Light Sleep

- Candidate: 继续处理后端 P0 部署后的 Sandbox Golden。决定不清理 2026-08-17 实体读卡器手测数据：这些数据仍有复盘和下一轮实体卡复验价值；Golden 应适应共享 Sandbox，而不是依赖空库。; Golden 改为按 `vendorBaseUrl=http://vendor-stub:8080` 复用已保留的供应商主机，仅删除自己创建的主机，避免误删手测主机。提交 `9e2c300`。; 日志审计发现 Golden 原先先通过 API 删除点位、后删会话，会触发外键 `DataIntegrityViolation` ERROR，尽管
  - confidence: 0.62
  - evidence: memory/2026-08-18.md:3-6
  - recalls: 0
  - status: staged
- Candidate: MySQL 权威数据第一批已完成，提交 `3f20035`：Flyway 9.22.3 接管数据库，V1 固化现有 baseline，V2 新建 `player_profiles`、`player_point_records`、`player_selections` 及影片配置/生效配置表，并补会话过期和影片恢复索引；旧 `schema.sql` / 忽略错误的 `schema-upgrade.sql` / 失效旧 V2 已移除。业务读写暂未切换，Redis 仍是玩家和影片配置来源。; 本地 MySQL 8 已分别验证空库执行 V1+V2、已有非空库 b
  - confidence: 0.62
  - evidence: memory/2026-08-18.md:11-14
  - recalls: 0
  - status: staged
- Candidate: Golden 新增 P0 核心部署断言：未激活 UID 搭配未注册 SN 时先返回 `DEVICE_NOT_REGISTERED`，并且只出现一条结构化拒绝日志。提交 `a53c84f`。; GitLab Pipeline 705 全绿（verify、build-image、deploy-sandbox、sandbox-golden、cleanup）；Golden 全链路成功，`DEVICE_NOT_REGISTERED` 日志恰好 1 条，清理外键错误 0、Redis 结构错误 0。backend、vendor-stub、MySQL、Redis 均 h
  - confidence: 0.62
  - evidence: memory/2026-08-18.md:7-10
  - recalls: 0
  - status: staged
- Candidate: MySQL 权威数据第三批已完成并推送，提交 `49e4c0c`：新增 `PlayerProgressService`，将游玩计次/最高分、选择结果及玩家详情、统计、覆盖率、checkout、影片输入统一切到 MySQL 的 `player_point_records` / `player_selections`；Redis 仅保留可丢弃的玩家档案兼容镜像。; 成绩回传的最高分更新与 `game_sessions` 从 `PROCESSING` 条件更新为 `PROCESSED` 已纳入同一 R2DBC 事务；故障注入测试确认会话条件更新失败时成绩同步回
  - confidence: 0.62
  - evidence: memory/2026-08-18.md:15-18
  - recalls: 0
  - status: staged
- Candidate: MySQL 下一批优先迁移影片序列配置与 active 配置，移除 `SL_CommonData` 的旧全局单键权威状态；随后补关键写失败传播、并发成绩回传/事务回滚、清空 Redis 后成就不变及 Golden Redis 副本丢失恢复测试。
  - confidence: 0.62
  - evidence: memory/2026-08-18.md:19-19
  - recalls: 0
  - status: staged
- Candidate: - MySQL 权威数据第一批已完成，提交 `3f20035`：Flyway 9.22.3 接管数据库，V1 固化现有 baseline，V2 新建 `player_profiles`、`player_point_records`、`player_selections` 及影片配置/生效配置表，并补会话过期和影片恢复索引；旧 `schema.sql` / 忽略错误的 `schema-upgrade.sql` / 失效旧 V2 已移除。业务读写暂未切换，Redis 仍是玩家和影片配置来源。 - 本地 MySQL 8 已分别验证空库执行 V1+V2、已有非空库 baseline 到 V1 后执行 V2、重复启动无迁移，后端 113 tests 全绿。Pipeline 708 全绿；Sandbox 现有非空库日志明确显示 baseline version 1、成功执行 V2 到 version 2，Golden 全链路通过，四个服务 healthy。 - MySQL 权威数据第二批已完成，提交 `1f63373`：手环档案、激活状态、状态查询、ACTIVE 校验和关闭切到 `player_profiles`，带乐观版本；Redis 仅同步兼容旧 `GameManager` 的档案镜像。按测试环境数据可重建的决策，不制作历史 Redis 玩家迁移/双写兼容层，现有测试玩家可重新激活。
  - confidence: 0.58
  - evidence: memory/2026-08-18.md:11-13
  - recalls: 2
  - status: staged
- Candidate: - 最新 Pipeline #678 全绿，verify/build-image/deploy-sandbox 均成功；本地全量测试增至 82+，工作区干净。 - 仍未完成且不可混淆：玩家成绩迁 MySQL 后的会话+成绩真原子事务、PROCESSING 崩溃恢复与 ≥7 天清理、RFID 失败码细分、签名/完整 Golden Fixture。 - 2026-08-06 复核前后端闭环能力：当前只支持核心子链路测试，尚不能宣称“前后端完整闭环验收”。后端 `6f64b6e` / Pipeline #678 全绿，已覆盖会话创建、供应商 ACCEPTED 后计次、V2 成绩幂等及主机模式/健康的服务级测试。 - 前端当前 `0565352`，本地 typecheck、10 条集成测试、构建、2 条 Playwright E2E 均通过，最新 GitHub CI（含部署与 Sandbox smoke）成功；但浏览器 E2E 使用 mock API，真实 Sandbox smoke 只覆盖登录、游戏组 CRUD、空影片配置完整性和 active 切换，未覆盖游客、RFID、供应商启动、成绩回传或成片。 - 前端仍使用点位级 `vendorBaseUrl/healthUrl/timeout/sharedSecret` 配置，尚未适配后端 `vendor_host`、`interactionPointCode + vendorHostId` 新契约，因此当前 UI 的游戏设备注册不能作为可靠的新后端验收路径。
  - confidence: 0.57
  - evidence: memory/2026-08-06.md:10-14
  - recalls: 2
  - status: staged
- Candidate: - MySQL 权威数据第三批已完成并推送，提交 `49e4c0c`：新增 `PlayerProgressService`，将游玩计次/最高分、选择结果及玩家详情、统计、覆盖率、checkout、影片输入统一切到 MySQL 的 `player_point_records` / `player_selections`；Redis 仅保留可丢弃的玩家档案兼容镜像。 - 成绩回传的最高分更新与 `game_sessions` 从 `PROCESSING` 条件更新为 `PROCESSED` 已纳入同一 R2DBC 事务；故障注入测试确认会话条件更新失败时成绩同步回滚，避免会话与成绩半成功。 - Sandbox Golden 已加入选择展项 fixture、MySQL 次数/最高分/会话断言，并在删除 Redis 玩家镜像后重新查询，验证玩家进度仍可从 MySQL 完整恢复。本地后端全量 118 tests 通过。 - GitLab Pipeline 710 已由 `49e4c0c` 触发；截至本次记忆刷新时最终状态尚未可靠确认，下一轮应先检查 verify、build、deploy、Golden、cleanup 及 Sandbox 四服务健康状态后再宣称收口。 - MySQL 下一批优先迁移影片序列配置与 active 配置，移除 `SL_CommonData` 的旧全局单键权威状态；随后补关键写失败传播、并发成绩回传/事务回滚、清空 Redis 后成就不变及 Golden Redis 副本丢失恢复测试。
  - confidence: 0.42
  - evidence: memory/2026-08-18.md:15-20
  - recalls: 1
  - status: staged
- Candidate: # 2026-08-18 - 继续处理后端 P0 部署后的 Sandbox Golden。决定不清理 2026-08-17 实体读卡器手测数据：这些数据仍有复盘和下一轮实体卡复验价值；Golden 应适应共享 Sandbox，而不是依赖空库。 - Golden 改为按 `vendorBaseUrl=http://vendor-stub:8080` 复用已保留的供应商主机，仅删除自己创建的主机，避免误删手测主机。提交 `9e2c300`。 - 日志审计发现 Golden 原先先通过 API 删除点位、后删会话，会触发外键 `DataIntegrityViolation` ERROR，尽管 SQL 兜底后任务仍成功。已调整为先清会话/夹具再走 API 回收，清理外键错误归零。提交 `5638591`。 - 日志审计还发现 Lua `cjson` 删除 Redis 视频任务会把共享 JSON 中空数组 `[]` 重编码成 `{}`，导致 `Expected BEGIN_ARRAY but was BEGIN_OBJECT`。已改为 Python JSON 读改写，保留数组/对象类型，缓存结构错误归零。提交 `8b4e541`。 - Golden 新增 P0 核心部署断言：未激活 UID 搭配未注册 SN 时先返回 `DEVICE_NOT_REGISTERED`，并且只出现一条结构化拒绝日志。提交 `a53c84f`。
  - confidence: 0.42
  - evidence: memory/2026-08-18.md:1-7
  - recalls: 1
  - status: staged
- Candidate: - MySQL 权威数据第二批已完成，提交 `1f63373`：手环档案、激活状态、状态查询、ACTIVE 校验和关闭切到 `player_profiles`，带乐观版本；Redis 仅同步兼容旧 `GameManager` 的档案镜像。按测试环境数据可重建的决策，不制作历史 Redis 玩家迁移/双写兼容层，现有测试玩家可重新激活。 - 本地真实 MySQL 验证首次激活、状态查询、重复激活和数据库记录正常；后端全量 114 tests 通过。Pipeline 709 全绿，Sandbox Golden 完整通过且四个服务 healthy；Flyway 确认 schema v2 无需迁移，运行日志仍只有 2 条故意空素材失败的预期 ERROR。
  - confidence: 0.71
  - evidence: memory/2026-08-18.md:13-15
  - recalls: 1
  - status: staged
- Candidate: - 七牛手测前置核对：项目 CI 已配置并掩码保存 `SANDBOX_QINIU_ACCESS_KEY` / `SANDBOX_QINIU_SECRET_KEY`，另有 Bucket/上传前缀变量；但 `deploy/compose.sandbox.yml` 仍固定关闭云上传并使用 `sandbox.invalid`，真实上传 Smoke 需要一次临时受控启用和测试对象清理。 - 为多 Sandbox 共用的 Docker 主机建立 `zzc-cloud-sandbox` 强制命令 SSH 网关：专用用户 `openclaw-zzc` 不在 docker 组；只读密钥仅可查看 4 个项目容器状态与 bounded logs；运维密钥额外只可重启 backend/vendor-stub。实测任意命令和只读密钥 restart 均被拒绝。 - 通过新日志网关复核现场刷卡：11:10～11:17 两个真实 SN `DI20240731000002` / `DI20260803000002` 均上报原始 `20B30E14`，该值只出现在 Netty LoggingHandler 入站日志；业务随后返回 `Bracelet is not activated` 和 `-1`，与十进制 UID 转换逻辑未变的判断一致。
  - confidence: 0.64
  - evidence: memory/2026-08-07.md:7-10
  - recalls: 3
  - status: staged
- Candidate: - Checkout/出片链路通过：两个并发 checkout 收敛到同一 `jobId/resultUrl`，手环进入 `CLOSED`，覆盖率快照 1/1、成绩 4、评级 A；FFmpeg 成功合成 `manual_game_A.mp4`，稳定结果地址正确返回 HTTP 303。Sandbox 当前关闭七牛上传，目标仍为 `sandbox.invalid`，因此本轮只验收本机合成与稳定 URL，未验收真实七牛上传。 - 本轮 Sandbox 测试数据暂不清理：卡 `0336507680` 已 CLOSED；SN `DI20260803000002` 仍路由到当前 vendor-stub；游戏组 `手测0817-游戏`、供应商主机 `Sandbox Vendor Stub`、点位 `MANUAL_GAME_0817`、全局生效影片配置 `手测0817-影片` 及 `manual_game.mp4`、`manual_game_A/B/C/D.mp4` 均保留，方便复盘。下轮实体卡测试需换新 UID/实体卡；若换游戏组，要先切换生效影片配置。 - 前后端回顾后已更新文档：前端 `README.md` 与 `docs/backend-api-plan.md`；后端新增 `docs/physical-reader-golden-2026-08-17.md`，并更新 `docs/backlog.md`、`docs/current-task-checklist.md` 与 Golden README。前端 `npm run verify` 通过（6 个测试文件、11 个测试、构建成功）；
  - confidence: 0.49
  - evidence: memory/2026-08-17.md:5-7
  - recalls: 1
  - status: staged
- Candidate: - OpenClaw sandbox browser 配置：在 `openclaw.json` 中已添加 `browser-sandbox` agent（sandbox.mode=all, backend=docker, browser.enabled=true）。Docker 基础镜像 `openclaw-sandbox:bookworm-slim` 和 sandbox browser 镜像 `openclaw-sandbox-browser:bookworm-slim` 均已本地构建完成。sandbox browser 镜像包含 Chromium、noVNC、Xvfb、x11vnc、websockify，ENTRYPOINT 脚本从 GitHub 源 `scripts/sandbox-browser-entrypoint.sh` 复制。gateway 已重启以加载新配置。待验证：sandbox browser 能否成功打开 `http://host.docker.internal:5173/` 并截图。 - sandbox browser 验证遇到的问题：(1) 初始 `browser-sandbox` agent 配置缺少 `sandbox.docker.image` 和 `sandbox.browser.image` 字段，使用默认镜像名但本地未构建；(2) 子会话 spawn 后因镜像不存在而立即失败（`Sandbox image not found: openclaw-sandbox:bookworm-slim`）；(3) 构建浏览器镜像时第一次因包下载量大(329
  - confidence: 0.56
  - evidence: memory/2026-07-06.md:26-27
  - recalls: 4
  - status: staged
- Candidate: - sandbox browser 验证遇到的问题：(1) 初始 `browser-sandbox` agent 配置缺少 `sandbox.docker.image` 和 `sandbox.browser.image` 字段，使用默认镜像名但本地未构建；(2) 子会话 spawn 后因镜像不存在而立即失败（`Sandbox image not found: openclaw-sandbox:bookworm-slim`）；(3) 构建浏览器镜像时第一次因包下载量大(329MB)被 OOM/超时 kill，第二次用分层构建成功。用户问"系统 Chrome headless 有啥不好"——回答：可以用但不隔离、依赖主机 Chrome 版本、策略可能拦截；sandbox browser 更安全可复现。 - 嗨咚木棉花 ISO 三体系认证：用户提供了煤矿洗选企业信息化平台项目的审核文字作为模板，要求改写成木棉花项目对应版本。项目时间改为 2026 年 1-6 月，已直接在飞书回复改编后的审核文字，未生成文件。关键替换：矿山企业→文旅/公园智能化体验；山西柳林金家庄煤业→广州市儿童公园；煤矿洗选企业信息化平台→嗨咚木棉花混合现实体验应用；功能模块从生产/设备/安全/质量改为 AR 互动/许愿树/木棉花/任务系统等；人员保持王国强/陈永祥/梁朋禹/郭伟；时间线 2024-5~8 → 2026-1~6。
  - confidence: 0.67
  - evidence: memory/2026-07-06.md:55-57
  - recalls: 1
  - status: staged
- Candidate: # 2026-08-04 - `zzc-cloud-server` 私有仓库：`https://gitlab.zzcreation.com/zzc2/zzc-cloud-server`，开发分支 `dev`，提交后由 GitLab CI 自动部署 Sandbox。 - 新建前端项目 `projects/zzc/zzc-sl-admin`，技术栈 Refine + Ant Design + React + Vite；当前已完成登录、JWT 刷新、玩家列表和详情，本地提交 `e28aa83`。 - 仓库归属已调整：`zzc-cloud-server` 继续以私有化 GitLab 为代码基线；`zzc-sl-admin` 改以私有 GitHub 仓库 `https://github.com/zzcreation/zzc-sl-admin` 为代码基线和 CI 平台。 - 前端部署 Cloudflare Pages 前仍需 HTTPS API 域名与 CORS 白名单，当前 Sandbox API 为 HTTP。 - `zzc-sl-admin` 已加入 Vitest + Testing Library + MSW 集成测试和 Playwright E2E。GitLab CI 曾因 Runner 拉取外部 Node 镜像 `unexpected EOF` 失败，后来虽通过本地专用镜像解决，但考虑 ECS 稳定性与存储，前端已删除 `.gitlab-ci.yml`、`ci/Dockerfile` 和系统 Chromium 适配，迁移到 GitHub Actions（提交 `9a0cb0
  - confidence: 0.72
  - evidence: memory/2026-08-04.md:1-7
  - recalls: 8
  - status: staged
- Candidate: - `zzc-cloud-server` 已完成前端阻塞型后端 Batch 1–4：GM 玩家分页/详情聚合与统一错误结构；GM 可按任意 configId 生成影片并保存配置快照；视频素材列表、鉴权预览/下载、上传覆盖、删除和配置完整性检查；设备档案扩展及存量数据库增量升级。对应关键提交为 `f2c0571`、`b67bc73`、`190dbc5`、`74fbcb7`、`4892606`，GitLab 流水线 #648–#651 均通过并部署 Sandbox。 - `zzc-cloud-server` 第一阶段 BUG-01～08 已清完：统一计分、刷卡不写 0 分、成绩 1–4 校验、按展项聚合并以全部配置展项为评级分母、影片 `_Z`/选项/默认片回退、空库建表与 refresh token 30 天秒制。关键提交 `70952fc`、`718a9bf`、`4a3a25f`、`7cbc6ef`。 - `zzc-sl-admin` 已完成游戏组、选择组和设备档案 CRUD，提交 `28a3d66`。设备表单包含 SN、类型/分组、楼层/分区/位置、三方与健康地址、超时、共享密钥、备注和启停状态；SN 与设备类型创建后锁定。
  - confidence: 0.72
  - evidence: memory/2026-08-04.md:9-11
  - recalls: 6
  - status: staged
- Candidate: - 后端 RFID P0 已在本地完成但尚未提交、尚未部署：校验顺序改为先查 SN/点位、后查手环；增加类型化拒绝结果（含 `DEVICE_NOT_REGISTERED`、未激活、CLOSED、供应商主机缺失、MANUAL、供应商启动失败）；预期拒绝仅由 Handler 记录一次结构化 WARN，内部故障仅记录一次 ERROR 且保留 cause；设备协议保持成功 ACK/失败 `-1` 不变。新增 10 个测试后全量为 109 tests，0 failure/error/skipped。下一步是部署前检查、提交/推送并通过 CI 部署 Sandbox，再用新卡复验未知 SN 优先、注册后成功、CLOSED 拒绝和单条日志。
  - confidence: 0.38
  - evidence: memory/2026-08-17.md:10-11
  - recalls: 1
  - status: staged
- Candidate: - 回顾确认三个后续事项：①真实设备原实现先查手环、后查 SN，未知设备会被“未激活”掩盖且一次失败最多重复记录三条日志；②DI20 当前协议只能表达成功/通用失败/超时，细分失败码需产品接受现状或协调厂商；③云上传关闭时仍标记 `COMPLETED` 并 303 到不可访问的 `sandbox.invalid`，应提供本地可访问地址或区分 `LOCAL_COMPLETED/PUBLISHED`。 - 已在开发机用户目录安装 Eclipse Temurin JDK 17.0.20+8 到 `~/.local/jdks/temurin-17`，并将 `JAVA_HOME/PATH` 持久化到 `~/.bashrc`。仓库 Maven Wrapper 3.9.11 能正确使用 Java 17；安装后后端基线 `./mvnw test` 为 99 tests 全绿。 - 后端 RFID P0 已在本地完成但尚未提交、尚未部署：校验顺序改为先查 SN/点位、后查手环；增加类型化拒绝结果（含 `DEVICE_NOT_REGISTERED`、未激活、CLOSED、供应商主机缺失、MANUAL、供应商启动失败）；预期拒绝仅由 Handler 记录一次结构化 WARN，内部故障仅记录一次 ERROR 且保留 cause；设备协议保持成功 ACK/失败 `-1` 不变。新增 10 个测试后全量为 109 tests，0 failure/error/skipped。下一步是部署前检查、提交/推送并通过 CI 部署 Sandbox，再用新卡复验未知 SN 优先、注册后成功、CLOSED 拒绝和单条日
  - confidence: 0.38
  - evidence: memory/2026-08-17.md:8-10
  - recalls: 1
  - status: staged
